services:
  aguapp:
    build: .
    container_name: aguapp
    restart: unless-stopped
    ports:
      - "3009:3001"
    env_file:
      - .env
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3001"]
      interval: 30s
      timeout: 10s
      retries: 3
