-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.aceptacion (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  id_diagnostico bigint,
  id_paciente bigint,
  generado boolean,
  fecha_diagnos timestamp with time zone,
  fecha_creado timestamp with time zone NOT NULL DEFAULT now(),
  fecha_actualizado timestamp with time zone,
  CONSTRAINT aceptacion_pkey PRIMARY KEY (id),
  CONSTRAINT aceptacion_id_diagnostico_fkey FOREIGN KEY (id_diagnostico) REFERENCES public.diagnostico(id),
  CONSTRAINT aceptacion_id_paciente_fkey FOREIGN KEY (id_paciente) REFERENCES public.pacientes(id)
);
CREATE TABLE public.agent_memory (
  id integer NOT NULL DEFAULT nextval('agent_memory_id_seq'::regclass),
  session_id character varying NOT NULL,
  message jsonb NOT NULL,
  CONSTRAINT agent_memory_pkey PRIMARY KEY (id)
);
CREATE TABLE public.agent_memory_interno (
  id integer NOT NULL DEFAULT nextval('agent_memory_interno_id_seq'::regclass),
  session_id character varying NOT NULL,
  message jsonb NOT NULL,
  CONSTRAINT agent_memory_interno_pkey PRIMARY KEY (id)
);
CREATE TABLE public.aplicaciones_cliente (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  fecha_actualizado timestamp with time zone DEFAULT now(),
  empresa_id bigint,
  aplicacion text,
  codigo text,
  estado text,
  licencias smallint,
  CONSTRAINT aplicaciones_cliente_pkey PRIMARY KEY (id),
  CONSTRAINT aplicaciones_cliente_empresa_id_fkey FOREIGN KEY (empresa_id) REFERENCES public.wp_empresa_perfil(id)
);
CREATE TABLE public.aplicaciones_dispositivos (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  id_control bigint,
  id_aplicacion bigint,
  ip text,
  vinculado boolean,
  fecha_vinculacion timestamp without time zone,
  CONSTRAINT aplicaciones_dispositivos_pkey PRIMARY KEY (id),
  CONSTRAINT aplicaciones_dispositivos_id_aplicacion_fkey FOREIGN KEY (id_aplicacion) REFERENCES public.aplicaciones_cliente(id)
);
CREATE TABLE public.artifact_stars (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  artifact_id uuid NOT NULL,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT artifact_stars_pkey PRIMARY KEY (id),
  CONSTRAINT artifact_stars_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id),
  CONSTRAINT artifact_stars_artifact_id_fkey FOREIGN KEY (artifact_id) REFERENCES public.artifacts(id)
);
CREATE TABLE public.artifact_versions (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  artifact_id uuid NOT NULL,
  content text NOT NULL,
  title text,
  description text,
  version_number integer NOT NULL,
  change_description text,
  is_auto_save boolean DEFAULT false,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT artifact_versions_pkey PRIMARY KEY (id),
  CONSTRAINT artifact_versions_artifact_id_fkey FOREIGN KEY (artifact_id) REFERENCES public.artifacts(id)
);
CREATE TABLE public.artifacts (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  session_id text,
  message_id uuid,
  title text NOT NULL DEFAULT 'Untitled Artifact'::text,
  content text NOT NULL,
  type text NOT NULL DEFAULT 'html'::text CHECK (type = ANY (ARRAY['html'::text, 'markdown'::text, 'svg'::text, 'mermaid'::text, 'react'::text, 'code'::text, 'research'::text])),
  language text,
  description text,
  tags ARRAY DEFAULT '{}'::text[],
  is_pinned boolean DEFAULT false,
  is_public boolean DEFAULT false,
  public_slug text UNIQUE,
  view_count integer DEFAULT 0,
  fork_count integer DEFAULT 0,
  forked_from uuid,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT artifacts_pkey PRIMARY KEY (id),
  CONSTRAINT artifacts_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id),
  CONSTRAINT artifacts_session_id_fkey FOREIGN KEY (session_id) REFERENCES adaptive_interface.chat_sessions(id),
  CONSTRAINT artifacts_message_id_fkey FOREIGN KEY (message_id) REFERENCES adaptive_interface.chat_messages(id),
  CONSTRAINT artifacts_forked_from_fkey FOREIGN KEY (forked_from) REFERENCES public.artifacts(id)
);
CREATE TABLE public.auditoria_historial (
  id bigint NOT NULL DEFAULT nextval('auditoria_historial_id_seq'::regclass),
  tabla_nombre text NOT NULL,
  columna_nombre text NOT NULL,
  fila_id bigint NOT NULL,
  valor_anterior text,
  valor_nuevo text,
  operacion text NOT NULL CHECK (operacion = ANY (ARRAY['INSERT'::text, 'UPDATE'::text, 'DELETE'::text])),
  usuario text DEFAULT CURRENT_USER,
  fecha_hora timestamp with time zone DEFAULT CURRENT_TIMESTAMP,
  metadata jsonb DEFAULT '{}'::jsonb,
  CONSTRAINT auditoria_historial_pkey PRIMARY KEY (id)
);
CREATE TABLE public.chatbot_conversations (
  id bigint NOT NULL DEFAULT nextval('chatbot_conversations_id_seq'::regclass),
  chatbot_id integer NOT NULL,
  empresa_id integer NOT NULL,
  external_conversation_id text NOT NULL,
  external_user_id text NOT NULL,
  external_user_name text,
  external_user_phone text,
  agente_id integer,
  agente_asignado_at timestamp with time zone,
  estado text DEFAULT 'active'::text CHECK (estado = ANY (ARRAY['active'::text, 'waiting'::text, 'closed'::text, 'transferred'::text])),
  prioridad integer DEFAULT 3 CHECK (prioridad >= 1 AND prioridad <= 5),
  contexto jsonb DEFAULT '{}'::jsonb,
  ultimo_mensaje text,
  ultimo_mensaje_at timestamp with time zone,
  mensajes_count integer DEFAULT 0,
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  closed_at timestamp with time zone,
  closed_reason text CHECK (closed_reason = ANY (ARRAY['completed'::text, 'timeout'::text, 'transferred'::text, 'spam'::text])),
  CONSTRAINT chatbot_conversations_pkey PRIMARY KEY (id),
  CONSTRAINT chatbot_conversations_chatbot_id_fkey FOREIGN KEY (chatbot_id) REFERENCES public.chatbot_integrations(id),
  CONSTRAINT chatbot_conversations_empresa_id_fkey FOREIGN KEY (empresa_id) REFERENCES public.wp_empresa_perfil(id),
  CONSTRAINT chatbot_conversations_agente_id_fkey FOREIGN KEY (agente_id) REFERENCES public.wp_agentes(id)
);
CREATE TABLE public.chatbot_integrations (
  id bigint NOT NULL DEFAULT nextval('chatbot_integrations_id_seq'::regclass),
  empresa_id integer NOT NULL,
  nombre text NOT NULL,
  tipo text NOT NULL CHECK (tipo = ANY (ARRAY['whatsapp'::text, 'facebook'::text, 'instagram'::text, 'telegram'::text, 'web'::text, 'api'::text])),
  webhook_url text,
  webhook_secret text,
  webhook_headers jsonb DEFAULT '{}'::jsonb,
  api_key_encrypted text,
  auth_type text DEFAULT 'bearer'::text CHECK (auth_type = ANY (ARRAY['bearer'::text, 'basic'::text, 'api_key'::text, 'none'::text])),
  agente_id integer,
  distribucion_mode text DEFAULT 'fixed'::text CHECK (distribucion_mode = ANY (ARRAY['fixed'::text, 'round_robin'::text, 'least_busy'::text, 'skill_based'::text])),
  respuesta_automatica boolean DEFAULT true,
  horario_atencion jsonb DEFAULT '{"schedule": {"monday": {"end": "18:00", "start": "09:00"}}, "timezone": "America/Mexico_City"}'::jsonb,
  fuera_horario_mensaje text DEFAULT 'Hola, en este momento estamos fuera de horario de atención. Te responderemos lo antes posible.'::text,
  activo boolean DEFAULT true,
  mensajes_recibidos integer DEFAULT 0,
  mensajes_enviados integer DEFAULT 0,
  ultimo_mensaje_at timestamp with time zone,
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  updated_at timestamp with time zone DEFAULT timezone('utc'::text, now()),
  CONSTRAINT chatbot_integrations_pkey PRIMARY KEY (id),
  CONSTRAINT chatbot_integrations_empresa_id_fkey FOREIGN KEY (empresa_id) REFERENCES public.wp_empresa_perfil(id),
  CONSTRAINT chatbot_integrations_agente_id_fkey FOREIGN KEY (agente_id) REFERENCES public.wp_agentes(id)
);
CREATE TABLE public.chatbot_messages (
  id bigint NOT NULL DEFAULT nextval('chatbot_messages_id_seq'::regclass),
  conversation_id integer NOT NULL,
  empresa_id integer NOT NULL,
  remitente text NOT NULL CHECK (remitente = ANY (ARRAY['user'::text, 'agent'::text, 'system'::text, 'bot'::text])),
  contenido text NOT NULL,
  tipo text DEFAULT 'text'::text CHECK (tipo = ANY (ARRAY['text'::text, 'image'::text, 'audio'::text, 'file'::text, 'location'::text, 'template'::text])),
  external_message_id text,
  metadata jsonb DEFAULT '{}'::jsonb,
  procesado boolean DEFAULT false,
  error_procesamiento text,
  enviado_externo boolean DEFAULT false,
  externo_error text,
  created_at timestamp with time zone NOT NULL DEFAULT timezone('utc'::text, now()),
  CONSTRAINT chatbot_messages_pkey PRIMARY KEY (id),
  CONSTRAINT chatbot_messages_conversation_id_fkey FOREIGN KEY (conversation_id) REFERENCES public.chatbot_conversations(id),
  CONSTRAINT chatbot_messages_empresa_id_fkey FOREIGN KEY (empresa_id) REFERENCES public.wp_empresa_perfil(id)
);
CREATE TABLE public.dau_tracked_users (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  username character varying NOT NULL UNIQUE,
  user_id character varying NOT NULL UNIQUE,
  about_them text,
  followers integer,
  is_active boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT dau_tracked_users_pkey PRIMARY KEY (id)
);
CREATE TABLE public.dau_user_tweets (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id character varying NOT NULL,
  status character varying DEFAULT 'Not done'::character varying,
  tweet_id character varying NOT NULL UNIQUE,
  tweet text NOT NULL,
  time_posted timestamp with time zone,
  views integer DEFAULT 0,
  favs integer DEFAULT 0,
  conversation_id character varying,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT dau_user_tweets_pkey PRIMARY KEY (id),
  CONSTRAINT dau_user_tweets_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.dau_tracked_users(user_id)
);
CREATE TABLE public.debug_events (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  created_at timestamp with time zone DEFAULT now(),
  source text NOT NULL,
  stage text NOT NULL,
  payload jsonb DEFAULT '{}'::jsonb,
  empresa_id integer,
  contacto_id integer,
  message_id text,
  CONSTRAINT debug_events_pkey PRIMARY KEY (id)
);
CREATE TABLE public.diagnostico (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  diagnostico text,
  procedimiento text,
  afectaciones text,
  notas text,
  id_paciente bigint,
  fecha_aceptado timestamp with time zone,
  fecha_diagnos timestamp with time zone,
  fecha_creado timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT diagnostico_pkey PRIMARY KEY (id),
  CONSTRAINT diagnostico_id_paciente_fkey FOREIGN KEY (id_paciente) REFERENCES public.pacientes(id)
);
CREATE TABLE public.drive_files (
  id bigint NOT NULL DEFAULT nextval('drive_files_id_seq'::regclass),
  file_id text NOT NULL UNIQUE,
  nombre text NOT NULL,
  url text NOT NULL,
  tipo_mime text,
  tamano text,
  fecha_modificacion timestamp with time zone,
  carpeta_origen text,
  carpeta_id text,
  metadata jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  traduccion text,
  contacto_id bigint,
  empresa_id bigint,
  carpeta_url text,
  url_storage text,
  descripccion text,
  doc_url text,
  contenido text,
  estado text,
  CONSTRAINT drive_files_pkey PRIMARY KEY (id),
  CONSTRAINT drive_files_contacto_id_fkey FOREIGN KEY (contacto_id) REFERENCES public.wp_contactos(id),
  CONSTRAINT drive_files_empresa_id_fkey FOREIGN KEY (empresa_id) REFERENCES public.wp_empresa_perfil(id)
);
CREATE TABLE public.evaluaciones (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  fecha_creada timestamp without time zone DEFAULT now(),
  evaluacion text,
  puntuacion integer,
  empresa_id bigint,
  agente_id bigint,
  periodo_evaluado jsonb,
  evaluacion_cruda text,
  CONSTRAINT evaluaciones_pkey PRIMARY KEY (id),
  CONSTRAINT evaluaciones_empresa_id_fkey FOREIGN KEY (empresa_id) REFERENCES public.wp_empresa_perfil(id),
  CONSTRAINT evaluaciones_agente_id_fkey FOREIGN KEY (agente_id) REFERENCES public.wp_agentes(id)
);
CREATE TABLE public.metricas (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  metrica text NOT NULL,
  valor numeric NOT NULL,
  fecha date NOT NULL DEFAULT CURRENT_DATE,
  meta jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone DEFAULT now(),
  empresa_id bigint,
  descripcion text,
  comparativa jsonb,
  CONSTRAINT metricas_pkey PRIMARY KEY (id),
  CONSTRAINT metricas_empresa_id_fkey FOREIGN KEY (empresa_id) REFERENCES public.wp_empresa_perfil(id)
);
CREATE TABLE public.minijuegos_jugadores (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  team_member_id bigint,
  organizacion_id uuid,
  nickname text,
  avatar_url text,
  xp integer DEFAULT 0,
  nivel integer DEFAULT 1,
  created_at timestamp with time zone DEFAULT now(),
  empresa_id bigint,
  CONSTRAINT minijuegos_jugadores_pkey PRIMARY KEY (id),
  CONSTRAINT minijuegos_jugadores_team_member_id_fkey FOREIGN KEY (team_member_id) REFERENCES public.wp_team_humano(id),
  CONSTRAINT minijuegos_jugadores_organizacion_id_fkey FOREIGN KEY (organizacion_id) REFERENCES public.minijuegos_organizaciones(id),
  CONSTRAINT minijuegos_jugadores_empresa_id_fkey FOREIGN KEY (empresa_id) REFERENCES public.wp_empresa_perfil(id)
);
CREATE TABLE public.minijuegos_organizaciones (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  nombre text NOT NULL,
  empresa_id bigint,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  is_active boolean DEFAULT true,
  CONSTRAINT minijuegos_organizaciones_pkey PRIMARY KEY (id),
  CONSTRAINT minijuegos_organizaciones_empresa_id_fkey FOREIGN KEY (empresa_id) REFERENCES public.wp_empresa_perfil(id)
);
CREATE TABLE public.monica_memories (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  content text NOT NULL,
  summary text,
  category text DEFAULT 'general'::text,
  importance integer DEFAULT 5 CHECK (importance >= 1 AND importance <= 10),
  metadata jsonb DEFAULT '{}'::jsonb,
  source text,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  embedding USER-DEFINED,
  CONSTRAINT monica_memories_pkey PRIMARY KEY (id)
);
CREATE TABLE public.monica_user_profile (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  key text NOT NULL UNIQUE,
  value text NOT NULL,
  confidence double precision DEFAULT 1.0,
  source text,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT monica_user_profile_pkey PRIMARY KEY (id)
);
CREATE TABLE public.n18_autonomy (
  id bigint NOT NULL DEFAULT nextval('n18_autonomy_id_seq'::regclass),
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  cron_name text NOT NULL,
  cron_id text,
  message_type text NOT NULL DEFAULT 'proactive'::text,
  message_summary text NOT NULL,
  diego_response text CHECK (diego_response = ANY (ARRAY['acted'::text, 'acknowledged'::text, 'ignored'::text, 'rejected'::text, 'pending'::text])),
  response_time_minutes integer,
  responded_at timestamp with time zone,
  week_number integer DEFAULT (EXTRACT(week FROM CURRENT_DATE))::integer,
  CONSTRAINT n18_autonomy_pkey PRIMARY KEY (id)
);
CREATE TABLE public.n18_autonomy_rules (
  id bigint NOT NULL DEFAULT nextval('n18_autonomy_rules_id_seq'::regclass),
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  cron_name text NOT NULL UNIQUE,
  current_level text NOT NULL DEFAULT 'normal'::text CHECK (current_level = ANY (ARRAY['aggressive'::text, 'normal'::text, 'conservative'::text, 'silent'::text])),
  ignore_rate numeric DEFAULT 0,
  act_rate numeric DEFAULT 0,
  total_sent integer DEFAULT 0,
  last_analysis_at timestamp with time zone,
  notes text,
  CONSTRAINT n18_autonomy_rules_pkey PRIMARY KEY (id)
);
CREATE TABLE public.n18_episodic (
  id bigint NOT NULL DEFAULT nextval('n18_episodic_id_seq'::regclass),
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  episode_date date NOT NULL DEFAULT CURRENT_DATE UNIQUE,
  summary text NOT NULL,
  decisions jsonb DEFAULT '[]'::jsonb,
  people_mentioned jsonb DEFAULT '[]'::jsonb,
  projects_updated jsonb DEFAULT '[]'::jsonb,
  tasks_assigned jsonb DEFAULT '[]'::jsonb,
  mood_context text,
  session_count integer DEFAULT 1,
  CONSTRAINT n18_episodic_pkey PRIMARY KEY (id)
);
CREATE TABLE public.n18_memory (
  id bigint NOT NULL DEFAULT nextval('n18_memory_id_seq'::regclass),
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  category text NOT NULL DEFAULT 'general'::text,
  title text NOT NULL,
  content text NOT NULL,
  importance integer NOT NULL DEFAULT 5 CHECK (importance >= 1 AND importance <= 10),
  source text DEFAULT 'conversation'::text,
  is_active boolean NOT NULL DEFAULT true,
  access_count integer NOT NULL DEFAULT 0,
  last_accessed_at timestamp with time zone,
  CONSTRAINT n18_memory_pkey PRIMARY KEY (id)
);
CREATE TABLE public.n8n_chat_histories (
  id integer NOT NULL DEFAULT nextval('n8n_chat_histories_id_seq'::regclass),
  session_id character varying NOT NULL,
  message jsonb NOT NULL,
  CONSTRAINT n8n_chat_histories_pkey PRIMARY KEY (id)
);
CREATE TABLE public.numero18_alerts (
  id bigint NOT NULL DEFAULT nextval('numero18_alerts_id_seq'::regclass),
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  source text NOT NULL DEFAULT 'monitor'::text,
  event_type text NOT NULL DEFAULT 'email'::text,
  priority text NOT NULL CHECK (priority = ANY (ARRAY['CRITICAL'::text, 'HIGH'::text, 'MEDIUM'::text, 'LOW'::text])),
  title text NOT NULL,
  content jsonb DEFAULT '{}'::jsonb,
  status text NOT NULL DEFAULT 'pending'::text CHECK (status = ANY (ARRAY['pending'::text, 'done'::text, 'dismissed'::text])),
  processed_at timestamp with time zone,
  outcome text,
  CONSTRAINT numero18_alerts_pkey PRIMARY KEY (id)
);
CREATE TABLE public.pacientes (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  nombre text,
  correo text,
  telefono text,
  empresa_id bigint,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  fecha_nacimiento timestamp without time zone,
  historia_medica jsonb,
  CONSTRAINT pacientes_pkey PRIMARY KEY (id),
  CONSTRAINT pacientes_empresa_id_fkey FOREIGN KEY (empresa_id) REFERENCES public.wp_empresa_perfil(id)
);
CREATE TABLE public.redaccion (
  id bigint NOT NULL DEFAULT nextval('redaccion_id_seq'::regclass),
  nombre character varying NOT NULL,
  url_doc character varying,
  tipo_id bigint NOT NULL,
  contacto_id bigint,
  estado character varying DEFAULT 'borrador'::character varying CHECK (estado::text = ANY (ARRAY['borrador'::character varying::text, 'en_revision'::character varying::text, 'publicado'::character varying::text, 'archivado'::character varying::text, 'preparando'::character varying::text])),
  descripcion text,
  created_at timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  contexto_structured jsonb,
  CONSTRAINT redaccion_pkey PRIMARY KEY (id),
  CONSTRAINT fk_redaccion_tipo FOREIGN KEY (tipo_id) REFERENCES public.redaccion_tipos(id),
  CONSTRAINT redaccion_contacto_id_fkey FOREIGN KEY (contacto_id) REFERENCES public.wp_contactos(id)
);
CREATE TABLE public.redaccion_detalle_historial (
  id bigint NOT NULL DEFAULT nextval('redaccion_detalle_historial_id_seq'::regclass),
  detalle_id bigint NOT NULL,
  empresa_id bigint NOT NULL,
  titulo text,
  contenido text,
  changed_by uuid,
  change_type text NOT NULL DEFAULT 'manual'::text,
  change_summary text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT redaccion_detalle_historial_pkey PRIMARY KEY (id),
  CONSTRAINT redaccion_detalle_historial_detalle_id_fkey FOREIGN KEY (detalle_id) REFERENCES public.redaccion_detalles(id),
  CONSTRAINT redaccion_detalle_historial_empresa_id_fkey FOREIGN KEY (empresa_id) REFERENCES public.wp_empresa_perfil(id),
  CONSTRAINT redaccion_detalle_historial_changed_by_fkey FOREIGN KEY (changed_by) REFERENCES auth.users(id)
);
CREATE TABLE public.redaccion_detalles (
  id bigint NOT NULL DEFAULT nextval('redaccion_detalles_id_seq'::regclass),
  titulo character varying NOT NULL,
  contenido text,
  orden integer NOT NULL,
  redaccion_id bigint NOT NULL,
  evaluacion numeric CHECK (evaluacion >= 0::numeric AND evaluacion <= 10::numeric),
  plan_seccion text,
  created_at timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  CONSTRAINT redaccion_detalles_pkey PRIMARY KEY (id),
  CONSTRAINT fk_redaccion_detalle FOREIGN KEY (redaccion_id) REFERENCES public.redaccion(id)
);
CREATE TABLE public.redaccion_tipos (
  id bigint NOT NULL DEFAULT nextval('redaccion_tipos_id_seq'::regclass),
  nombre character varying NOT NULL,
  partes integer DEFAULT 1,
  instrucciones text,
  longitud integer,
  objetivo text,
  created_at timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  updated_at timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  requerimientos text,
  empresa_id bigint,
  CONSTRAINT redaccion_tipos_pkey PRIMARY KEY (id),
  CONSTRAINT redaccion_tipos_empresa_id_fkey FOREIGN KEY (empresa_id) REFERENCES public.wp_empresa_perfil(id)
);
CREATE TABLE public.reporte_preliminar (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  telefono text,
  data text,
  CONSTRAINT reporte_preliminar_pkey PRIMARY KEY (id)
);
CREATE TABLE public.shortened_links (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  type character varying NOT NULL,
  uuid uuid NOT NULL DEFAULT gen_random_uuid() UNIQUE,
  redirect_url text NOT NULL,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  clicks integer DEFAULT 0,
  is_active boolean DEFAULT true,
  CONSTRAINT shortened_links_pkey PRIMARY KEY (id)
);
CREATE TABLE public.sintetico_ejecuciones (
  id bigint NOT NULL DEFAULT nextval('sintetico_ejecuciones_id_seq'::regclass),
  perfil_id bigint,
  escenario_id bigint,
  session_id character varying UNIQUE,
  estado character varying DEFAULT 'iniciado'::character varying,
  total_mensajes integer DEFAULT 0,
  resultado jsonb DEFAULT '{}'::jsonb,
  objetivo_logrado boolean DEFAULT false,
  started_at timestamp with time zone DEFAULT now(),
  completed_at timestamp with time zone,
  numero_sintetico character varying,
  CONSTRAINT sintetico_ejecuciones_pkey PRIMARY KEY (id),
  CONSTRAINT sintetico_ejecuciones_perfil_id_fkey FOREIGN KEY (perfil_id) REFERENCES public.sintetico_perfiles(id),
  CONSTRAINT sintetico_ejecuciones_escenario_id_fkey FOREIGN KEY (escenario_id) REFERENCES public.sintetico_escenarios(id),
  CONSTRAINT sintetico_ejecuciones_numero_sintetico_fkey FOREIGN KEY (numero_sintetico) REFERENCES public.sintetico_perfiles(numero_sintetico)
);
CREATE TABLE public.sintetico_escenarios (
  id bigint NOT NULL DEFAULT nextval('sintetico_escenarios_id_seq'::regclass),
  nombre character varying NOT NULL UNIQUE,
  empresa_id bigint NOT NULL,
  agente_id bigint NOT NULL,
  numero_id bigint NOT NULL,
  servicio jsonb NOT NULL,
  max_mensajes integer DEFAULT 15,
  activo boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  id_perfil bigint,
  correo_informe text,
  CONSTRAINT sintetico_escenarios_pkey PRIMARY KEY (id),
  CONSTRAINT sintetico_escenarios_empresa_id_fkey FOREIGN KEY (empresa_id) REFERENCES public.wp_empresa_perfil(id),
  CONSTRAINT sintetico_escenarios_agente_id_fkey FOREIGN KEY (agente_id) REFERENCES public.wp_agentes(id),
  CONSTRAINT sintetico_escenarios_numero_id_fkey FOREIGN KEY (numero_id) REFERENCES public.wp_numeros(id),
  CONSTRAINT sintetico_escenarios_id_perfil_fkey FOREIGN KEY (id_perfil) REFERENCES public.sintetico_perfiles(id)
);
CREATE TABLE public.sintetico_perfiles (
  id bigint NOT NULL DEFAULT nextval('sintetico_perfiles_id_seq'::regclass),
  nombre character varying NOT NULL,
  numero_sintetico character varying UNIQUE,
  personalidad jsonb NOT NULL,
  activo boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  agente_id bigint,
  CONSTRAINT sintetico_perfiles_pkey PRIMARY KEY (id)
);
CREATE TABLE public.sofia_candidatos (
  id bigint NOT NULL DEFAULT nextval('sofia_candidatos_id_seq'::regclass),
  contacto_id bigint,
  empresa_id integer DEFAULT 4,
  vacante text,
  nombre_completo text,
  email text,
  telefono text,
  educacion text,
  experiencia_anos numeric,
  descripcion_experiencia text,
  ubicacion text,
  pais text,
  idiomas text,
  habilidades text,
  cv_url text,
  cv_enviado boolean DEFAULT false,
  salario_expectativa text,
  moneda_salario character varying DEFAULT 'USD'::character varying,
  salario_usd_estimado numeric,
  score integer DEFAULT 0,
  calificacion character varying DEFAULT 'evaluando'::character varying,
  comentario_sofia text,
  metadata_original jsonb DEFAULT '{}'::jsonb,
  fecha_aplicacion timestamp with time zone DEFAULT now(),
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT sofia_candidatos_pkey PRIMARY KEY (id)
);
CREATE TABLE public.system_actions (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  name text NOT NULL UNIQUE,
  display_name text NOT NULL,
  description text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT system_actions_pkey PRIMARY KEY (id)
);
CREATE TABLE public.system_modules (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  name text NOT NULL UNIQUE,
  display_name text NOT NULL,
  description text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT system_modules_pkey PRIMARY KEY (id)
);
CREATE TABLE public.system_permissions (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  role_id bigint NOT NULL,
  module_id bigint NOT NULL,
  actions jsonb NOT NULL DEFAULT '[]'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT system_permissions_pkey PRIMARY KEY (id),
  CONSTRAINT system_permissions_role_id_fkey FOREIGN KEY (role_id) REFERENCES public.system_roles(id),
  CONSTRAINT system_permissions_module_id_fkey FOREIGN KEY (module_id) REFERENCES public.system_modules(id)
);
CREATE TABLE public.system_roles (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  name text NOT NULL,
  description text,
  enterprise_id bigint,
  is_super_admin boolean NOT NULL DEFAULT false,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  active boolean DEFAULT true,
  CONSTRAINT system_roles_pkey PRIMARY KEY (id),
  CONSTRAINT system_roles_enterprise_id_fkey FOREIGN KEY (enterprise_id) REFERENCES public.wp_empresa_perfil(id)
);
CREATE TABLE public.system_users (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  auth_uid uuid DEFAULT auth.uid(),
  enterprise_id bigint,
  name text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  team_humano_id bigint,
  phone text,
  role_id bigint,
  role text,
  CONSTRAINT system_users_pkey PRIMARY KEY (id),
  CONSTRAINT system_users_team_humano_id_fkey1 FOREIGN KEY (team_humano_id) REFERENCES public.wp_team_humano(id),
  CONSTRAINT system_users_role_id_fkey FOREIGN KEY (role_id) REFERENCES public.system_roles(id),
  CONSTRAINT system_users_enterprise_id_fkey FOREIGN KEY (enterprise_id) REFERENCES public.wp_empresa_perfil(id)
);
CREATE TABLE public.team_groups (
  id bigint NOT NULL DEFAULT nextval('team_groups_id_seq'::regclass),
  empresa_id bigint NOT NULL,
  name text NOT NULL,
  slug text NOT NULL,
  icon text DEFAULT 'Users'::text,
  color text DEFAULT 'blue'::text,
  sort_order integer DEFAULT 0,
  is_active boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT team_groups_pkey PRIMARY KEY (id),
  CONSTRAINT team_groups_empresa_id_fkey FOREIGN KEY (empresa_id) REFERENCES public.wp_empresa_perfil(id)
);
CREATE TABLE public.team_invitations (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  email text NOT NULL UNIQUE,
  team_humano_id bigint NOT NULL,
  invitation_token uuid NOT NULL DEFAULT gen_random_uuid(),
  status text NOT NULL DEFAULT 'pending'::text CHECK (status = ANY (ARRAY['pending'::text, 'accepted'::text, 'expired'::text])),
  invited_by uuid,
  invited_at timestamp with time zone NOT NULL DEFAULT now(),
  expires_at timestamp with time zone NOT NULL DEFAULT (now() + '7 days'::interval),
  accepted_at timestamp with time zone,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT team_invitations_pkey PRIMARY KEY (id),
  CONSTRAINT team_invitations_team_humano_id_fkey FOREIGN KEY (team_humano_id) REFERENCES public.wp_team_humano(id),
  CONSTRAINT team_invitations_invited_by_fkey FOREIGN KEY (invited_by) REFERENCES auth.users(id)
);
CREATE TABLE public.temp_contactos_sin_citas (
  id bigint,
  telefono text,
  nombre text,
  apellido text,
  email text,
  fecha_registro timestamp with time zone,
  ultima_interaccion timestamp with time zone,
  origen text,
  notas text,
  is_active boolean,
  created_at timestamp with time zone,
  updated_at timestamp with time zone,
  subscriber_id text,
  avatar_url text,
  metadata jsonb,
  empresa_id bigint,
  etapa_emocional text,
  timezone text,
  es_calificado text,
  estado text,
  url_drive text,
  link_stripe text,
  team_humano_id bigint,
  etapa_embudo integer,
  informe_gamma text
);
CREATE TABLE public.test_escritura (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  original text,
  modificado text,
  hora timestamp with time zone,
  id_ejecucion bigint,
  nombre_wf text,
  CONSTRAINT test_escritura_pkey PRIMARY KEY (id)
);
CREATE TABLE public.test_respuestas_agente_evaluador (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL UNIQUE,
  input_cliente text,
  output_agente text,
  decision_evaluador text,
  nombre_agente text,
  emrpesa text,
  id_conversacion bigint,
  id_ejecucion bigint,
  motivo text,
  hora timestamp with time zone,
  CONSTRAINT test_respuestas_agente_evaluador_pkey PRIMARY KEY (id),
  CONSTRAINT test_respuestas_agente_evaluador_id_conversacion_fkey FOREIGN KEY (id_conversacion) REFERENCES public.wp_contactos(id)
);
CREATE TABLE public.training_courses (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  empresa_id bigint NOT NULL,
  titulo character varying NOT NULL,
  descripcion text,
  categoria character varying,
  dificultad character varying DEFAULT 'intermedio'::character varying CHECK (dificultad::text = ANY (ARRAY['principiante'::character varying, 'intermedio'::character varying, 'avanzado'::character varying]::text[])),
  duracion_estimada_min integer DEFAULT 15,
  portada_url text,
  color_tema character varying DEFAULT '#6366f1'::character varying,
  is_public boolean DEFAULT false,
  is_active boolean DEFAULT true,
  orden integer DEFAULT 0,
  created_by uuid,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT training_courses_pkey PRIMARY KEY (id),
  CONSTRAINT training_courses_empresa_id_fkey FOREIGN KEY (empresa_id) REFERENCES public.wp_empresa_perfil(id),
  CONSTRAINT training_courses_created_by_fkey FOREIGN KEY (created_by) REFERENCES auth.users(id)
);
CREATE TABLE public.training_lessons (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  course_id uuid NOT NULL,
  orden integer NOT NULL DEFAULT 0,
  titulo character varying NOT NULL,
  contenido_intro text,
  imagen_url text,
  tiempo_estimado_seg integer DEFAULT 180,
  xp_reward integer DEFAULT 10,
  xp_perfect_bonus integer DEFAULT 25,
  max_hearts integer DEFAULT 5,
  is_active boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT training_lessons_pkey PRIMARY KEY (id),
  CONSTRAINT training_lessons_course_id_fkey FOREIGN KEY (course_id) REFERENCES public.training_courses(id)
);
CREATE TABLE public.training_questions (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  lesson_id uuid NOT NULL,
  tipo character varying NOT NULL CHECK (tipo::text = ANY (ARRAY['multiple_choice'::character varying, 'true_false'::character varying, 'order_steps'::character varying, 'fill_blank'::character varying, 'roleplay'::character varying]::text[])),
  pregunta text NOT NULL,
  opciones jsonb,
  respuesta_correcta text NOT NULL,
  explicacion text,
  hint text,
  dificultad integer DEFAULT 1 CHECK (dificultad >= 1 AND dificultad <= 3),
  ai_generated boolean DEFAULT false,
  ai_context jsonb,
  orden integer DEFAULT 0,
  is_active boolean DEFAULT true,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT training_questions_pkey PRIMARY KEY (id),
  CONSTRAINT training_questions_lesson_id_fkey FOREIGN KEY (lesson_id) REFERENCES public.training_lessons(id)
);
CREATE TABLE public.training_streaks (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  team_member_id bigint NOT NULL UNIQUE,
  current_streak integer DEFAULT 0,
  longest_streak integer DEFAULT 0,
  last_activity_date date,
  streak_frozen_until date,
  freeze_count_used integer DEFAULT 0,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT training_streaks_pkey PRIMARY KEY (id),
  CONSTRAINT training_streaks_team_member_id_fkey FOREIGN KEY (team_member_id) REFERENCES public.wp_team_humano(id)
);
CREATE TABLE public.training_user_progress (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  team_member_id bigint NOT NULL,
  lesson_id uuid NOT NULL,
  status character varying DEFAULT 'not_started'::character varying CHECK (status::text = ANY (ARRAY['not_started'::character varying, 'in_progress'::character varying, 'completed'::character varying, 'failed'::character varying]::text[])),
  score integer,
  attempts integer DEFAULT 0,
  best_score integer,
  time_spent_sec integer DEFAULT 0,
  questions_correct integer DEFAULT 0,
  questions_total integer DEFAULT 0,
  local_checkpoint jsonb,
  last_sync_at timestamp with time zone,
  started_at timestamp with time zone,
  completed_at timestamp with time zone,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT training_user_progress_pkey PRIMARY KEY (id),
  CONSTRAINT training_user_progress_team_member_id_fkey FOREIGN KEY (team_member_id) REFERENCES public.wp_team_humano(id),
  CONSTRAINT training_user_progress_lesson_id_fkey FOREIGN KEY (lesson_id) REFERENCES public.training_lessons(id)
);
CREATE TABLE public.transcripciones (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  grant_id text,
  transcripcion character varying,
  notetaker_id character varying,
  duracion numeric,
  resumen text,
  cita_id bigint,
  resumen_cita text,
  reunion_id text,
  CONSTRAINT transcripciones_pkey PRIMARY KEY (id),
  CONSTRAINT transcripciones_cita_id_fkey FOREIGN KEY (cita_id) REFERENCES public.wp_citas(id)
);
CREATE TABLE public.usuarios_consentimientos (
  id integer NOT NULL DEFAULT nextval('smile_consentimientos_id_seq'::regclass),
  session_id uuid NOT NULL,
  timestamp timestamp without time zone NOT NULL,
  language character varying NOT NULL,
  id_cliente character varying,
  patient_dob date,
  non_refund_ack boolean,
  delivery_delay_terms_ack boolean,
  cementation_change_8days_ack boolean,
  punctuality_reschedule_ack boolean,
  veneer_8days_ack boolean,
  anesthesia_risks_ack boolean,
  preop_fasting_6h_ack boolean,
  patient_responsibilities_ack boolean,
  no_guarantee_ack boolean,
  extraction_irreversible_ack boolean,
  cementation_satisfaction_ack boolean,
  post_care_ack boolean,
  diagnosis_teeth_non_restorable ARRAY,
  diagnosis_missing_teeth ARRAY,
  planned_procedures ARRAY,
  graft_type character varying,
  anesthesia_choice character varying,
  not_satisfied_notes text,
  missing_required ARRAY,
  empresa_id integer UNIQUE,
  patient_name character varying,
  metadata text,
  CONSTRAINT usuarios_consentimientos_pkey PRIMARY KEY (id)
);
CREATE TABLE public.wp_actividades_log (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  tipo text NOT NULL,
  accion text NOT NULL,
  descripcion text,
  agente_id bigint,
  empresa_id bigint,
  contacto_id bigint,
  entidad_tipo text,
  datos_antes jsonb,
  datos_despues jsonb,
  fecha_creacion timestamp with time zone NOT NULL DEFAULT now(),
  usuario_id uuid,
  entidad_id text,
  ip_origen text,
  user_agent text,
  tipo_valido text,
  CONSTRAINT wp_actividades_log_pkey PRIMARY KEY (id),
  CONSTRAINT wp_actividades_log_contacto_id_fkey FOREIGN KEY (contacto_id) REFERENCES public.wp_contactos(id),
  CONSTRAINT wp_actividades_log_agente_id_fkey FOREIGN KEY (agente_id) REFERENCES public.wp_agentes(id),
  CONSTRAINT wp_actividades_log_empresa_id_fkey FOREIGN KEY (empresa_id) REFERENCES public.wp_empresa_perfil(id)
);
CREATE TABLE public.wp_agente_roles (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  nombre_rol text,
  instrucciones_rol text,
  fecha_creacion timestamp with time zone NOT NULL DEFAULT now(),
  fecha_actualizacion timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT wp_agente_roles_pkey PRIMARY KEY (id)
);
CREATE TABLE public.wp_agente_tools (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  agente_id bigint NOT NULL,
  tool_id bigint NOT NULL,
  activo boolean NOT NULL DEFAULT true,
  prioridad integer DEFAULT 0,
  configuracion jsonb,
  fecha_asignacion timestamp with time zone NOT NULL DEFAULT now(),
  fecha_actualizacion timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT wp_agente_tools_pkey PRIMARY KEY (id),
  CONSTRAINT wp_agente_tools_agente_id_fkey FOREIGN KEY (agente_id) REFERENCES public.wp_agentes(id),
  CONSTRAINT wp_agente_tools_tool_id_fkey FOREIGN KEY (tool_id) REFERENCES public.wp_mcp_tools_catalog(id)
);
CREATE TABLE public.wp_agentes (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  nombre_agente text NOT NULL,
  instrucciones text,
  comportamiento text,
  restricciones text,
  rol text,
  empresa_id bigint,
  formato_respuesta text,
  areas_de_expertise text,
  uso_de_emojis text,
  prompt_personalizado text,
  idioma text DEFAULT 'es'::text,
  url_imagen_agente text,
  fecha_creacion timestamp with time zone NOT NULL DEFAULT now(),
  fecha_actualizacion timestamp with time zone NOT NULL DEFAULT now(),
  manejo_herramientas text,
  instrucciones_mensajes text,
  url_videos text,
  llm text DEFAULT 'google/gemini-2.5-flash'::text,
  mcp_url text DEFAULT 'https://marketia.app.n8n.cloud/mcp/aa0f6b46-153c-48c2-ba2f-general'::text,
  instrucciones_multimedia text,
  id_rol bigint,
  metadata_contacto jsonb,
  archivado boolean DEFAULT false,
  CONSTRAINT wp_agentes_pkey PRIMARY KEY (id),
  CONSTRAINT wp_agentes_empresa_id_fkey FOREIGN KEY (empresa_id) REFERENCES public.wp_empresa_perfil(id),
  CONSTRAINT wp_agentes_id_rol_fkey FOREIGN KEY (id_rol) REFERENCES public.wp_agente_roles(id)
);
CREATE TABLE public.wp_auditoria (
  id bigint NOT NULL DEFAULT nextval('wp_auditoria_id_seq'::regclass),
  tabla text NOT NULL,
  registro_id bigint NOT NULL,
  campo text NOT NULL,
  valor_anterior text,
  valor_nuevo text,
  usuario_id bigint,
  usuario_nombre text,
  fecha timestamp with time zone DEFAULT now(),
  accion text DEFAULT 'UPDATE'::text,
  mensaje_commit text,
  CONSTRAINT wp_auditoria_pkey PRIMARY KEY (id)
);
CREATE TABLE public.wp_citas (
  id bigint NOT NULL DEFAULT nextval('wp_citas_id_seq'::regclass),
  contacto_id bigint,
  empresa_id bigint,
  fecha_hora timestamp with time zone,
  duracion integer,
  titulo text,
  ubicacion text,
  estado text DEFAULT 'pendiente'::text,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  event_id text UNIQUE,
  ultima_sincronizacion timestamp with time zone,
  team_humano_id bigint,
  timezone_cliente text DEFAULT ''::text,
  duracion_buffer integer DEFAULT 15,
  preguntas_calendario jsonb,
  descripcion text,
  sincronizacion text DEFAULT 'pendiente'::text,
  metadata jsonb,
  cuestionario_asesor jsonb,
  evaluacion_asesor jsonb,
  notificaciones jsonb,
  resumen_conversacion text,
  CONSTRAINT wp_citas_pkey PRIMARY KEY (id),
  CONSTRAINT wp_citas_contacto_id_fkey FOREIGN KEY (contacto_id) REFERENCES public.wp_contactos(id),
  CONSTRAINT wp_citas_team_humano_id_fkey FOREIGN KEY (team_humano_id) REFERENCES public.wp_team_humano(id),
  CONSTRAINT wp_citas_empresa_id_fkey FOREIGN KEY (empresa_id) REFERENCES public.wp_empresa_perfil(id)
);
CREATE TABLE public.wp_citas_participantes (
  id bigint NOT NULL DEFAULT nextval('wp_citas_participantes_id_seq'::regclass),
  cita_id bigint NOT NULL,
  team_humano_id bigint NOT NULL,
  rol text NOT NULL DEFAULT 'invitado'::text,
  estado_rsvp text DEFAULT 'pendiente'::text,
  email text,
  added_by text DEFAULT 'manual'::text,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT wp_citas_participantes_pkey PRIMARY KEY (id),
  CONSTRAINT wp_citas_participantes_cita_id_fkey FOREIGN KEY (cita_id) REFERENCES public.wp_citas(id),
  CONSTRAINT wp_citas_participantes_team_humano_id_fkey FOREIGN KEY (team_humano_id) REFERENCES public.wp_team_humano(id)
);
CREATE TABLE public.wp_contacto_estado_embudo (
  id bigint NOT NULL DEFAULT nextval('wp_contacto_estado_embudo_id_seq'::regclass),
  contacto_id bigint NOT NULL UNIQUE,
  empresa_id bigint NOT NULL,
  etapa_anterior bigint,
  etapa_actual bigint NOT NULL CHECK (etapa_actual > 0),
  team_humano_id bigint,
  origen_cambio text DEFAULT 'sistema'::text CHECK (origen_cambio = ANY (ARRAY['sistema'::text, 'agente_ia'::text, 'manual'::text, 'team_humano'::text, 'webhook'::text, 'api'::text])),
  notas text,
  metadata jsonb DEFAULT '{}'::jsonb,
  fecha_ingreso_embudo timestamp with time zone NOT NULL DEFAULT now(),
  fecha_ultimo_cambio timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT wp_contacto_estado_embudo_pkey PRIMARY KEY (id),
  CONSTRAINT wp_contacto_estado_embudo_contacto_fkey FOREIGN KEY (contacto_id) REFERENCES public.wp_contactos(id),
  CONSTRAINT wp_contacto_estado_embudo_empresa_fkey FOREIGN KEY (empresa_id) REFERENCES public.wp_empresa_perfil(id),
  CONSTRAINT wp_contacto_estado_embudo_team_fkey FOREIGN KEY (team_humano_id) REFERENCES public.wp_team_humano(id),
  CONSTRAINT wp_contacto_estado_embudo_etapa_actual_fkey FOREIGN KEY (etapa_actual) REFERENCES public.wp_empresa_embudo(id),
  CONSTRAINT wp_contacto_estado_embudo_etapa_anterior_fkey FOREIGN KEY (etapa_anterior) REFERENCES public.wp_empresa_embudo(id)
);
CREATE TABLE public.wp_contacto_team_asignaciones (
  id bigint NOT NULL DEFAULT nextval('wp_contacto_team_asignaciones_id_seq'::regclass),
  contacto_id bigint NOT NULL,
  team_humano_id bigint NOT NULL,
  es_principal boolean DEFAULT false,
  rol_asignacion character varying CHECK (rol_asignacion IS NULL OR (rol_asignacion::text = ANY (ARRAY['principal'::character varying, 'colaborador'::character varying, 'observador'::character varying]::text[]))),
  asignado_por bigint,
  empresa_id bigint NOT NULL,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT wp_contacto_team_asignaciones_pkey PRIMARY KEY (id),
  CONSTRAINT wp_contacto_team_asignaciones_contacto_id_fkey FOREIGN KEY (contacto_id) REFERENCES public.wp_contactos(id),
  CONSTRAINT wp_contacto_team_asignaciones_team_humano_id_fkey FOREIGN KEY (team_humano_id) REFERENCES public.wp_team_humano(id),
  CONSTRAINT wp_contacto_team_asignaciones_asignado_por_fkey FOREIGN KEY (asignado_por) REFERENCES public.wp_team_humano(id),
  CONSTRAINT wp_contacto_team_asignaciones_empresa_id_fkey FOREIGN KEY (empresa_id) REFERENCES public.wp_empresa_perfil(id)
);
CREATE TABLE public.wp_contactos (
  id bigint NOT NULL DEFAULT nextval('wp_contactos_id_seq'::regclass),
  telefono text,
  nombre text,
  apellido text,
  email text,
  fecha_registro timestamp with time zone NOT NULL DEFAULT now(),
  ultima_interaccion timestamp with time zone DEFAULT now(),
  origen text,
  notas text,
  is_active boolean DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  subscriber_id text,
  avatar_url text,
  metadata jsonb,
  empresa_id bigint,
  etapa_emocional text,
  timezone text DEFAULT 'Por definir'::text,
  es_calificado text DEFAULT 'evaluando'::text CHECK (es_calificado = ANY (ARRAY['si'::text, 'no'::text, 'evaluando'::text])),
  estado text DEFAULT 'prospecto'::text,
  url_drive text,
  link_stripe text,
  team_humano_id bigint,
  etapa_embudo bigint,
  informe_gamma text,
  suscripcion boolean DEFAULT true,
  paused_until timestamp with time zone,
  CONSTRAINT wp_contactos_pkey PRIMARY KEY (id),
  CONSTRAINT wp_contactos_team_humano_id_fkey FOREIGN KEY (team_humano_id) REFERENCES public.wp_team_humano(id),
  CONSTRAINT wp_contactos_empresa_id_fkey FOREIGN KEY (empresa_id) REFERENCES public.wp_empresa_perfil(id),
  CONSTRAINT wp_contactos_etapa_embudo_fkey FOREIGN KEY (etapa_embudo) REFERENCES public.wp_empresa_embudo(id)
);
CREATE TABLE public.wp_contactos_auditoria (
  id bigint NOT NULL DEFAULT nextval('wp_contactos_auditoria_id_seq'::regclass),
  contacto_id bigint NOT NULL,
  telefono_viejo text,
  telefono_nuevo text,
  empresa_id bigint,
  canal_comunicacion text,
  accion text NOT NULL,
  resultado text NOT NULL,
  mensaje text,
  creado_en timestamp with time zone DEFAULT now(),
  CONSTRAINT wp_contactos_auditoria_pkey PRIMARY KEY (id)
);
CREATE TABLE public.wp_contactos_merge_log (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  empresa_id bigint NOT NULL,
  primary_contact_id bigint NOT NULL,
  secondary_contact_id bigint NOT NULL,
  secondary_snapshot jsonb NOT NULL,
  field_choices jsonb NOT NULL DEFAULT '{}'::jsonb,
  tables_updated jsonb NOT NULL DEFAULT '{}'::jsonb,
  merged_by bigint,
  merged_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT wp_contactos_merge_log_pkey PRIMARY KEY (id),
  CONSTRAINT wp_contactos_merge_log_empresa_id_fkey FOREIGN KEY (empresa_id) REFERENCES public.wp_empresa_perfil(id),
  CONSTRAINT wp_contactos_merge_log_merged_by_fkey FOREIGN KEY (merged_by) REFERENCES public.wp_team_humano(id)
);
CREATE TABLE public.wp_contactos_nota (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  descripcion text,
  contacto_id bigint,
  team_humano_id bigint,
  titulo text,
  etiquetas jsonb DEFAULT '[]'::jsonb,
  es_fijado boolean DEFAULT false,
  archivos_urls ARRAY DEFAULT '{}'::text[],
  visible_ia boolean DEFAULT true,
  CONSTRAINT wp_contactos_nota_pkey PRIMARY KEY (id),
  CONSTRAINT wp_contactos_nota_contacto_id_fkey FOREIGN KEY (contacto_id) REFERENCES public.wp_contactos(id),
  CONSTRAINT wp_contactos_nota_team_humano_id_fkey FOREIGN KEY (team_humano_id) REFERENCES public.wp_team_humano(id)
);
CREATE TABLE public.wp_conversaciones (
  id bigint NOT NULL DEFAULT nextval('wp_conversaciones_id_seq'::regclass),
  agente_id bigint,
  contacto_id bigint NOT NULL,
  fecha_inicio timestamp with time zone NOT NULL DEFAULT now(),
  metadata jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  numero_id bigint,
  canal text NOT NULL,
  empresa_id bigint,
  seguimiento text,
  resumen text,
  evaluacion numeric,
  fecha_analisis timestamp with time zone,
  subscriber_id_redes text,
  fecha_ultimo_mensaje_usuario timestamp with time zone DEFAULT (now() AT TIME ZONE 'utc'::text),
  inteligencia_conversacional text,
  ultimo_mensaje_at timestamp with time zone,
  ultimo_mensaje_contenido text,
  ultimo_mensaje_remitente text,
  CONSTRAINT wp_conversaciones_pkey PRIMARY KEY (id),
  CONSTRAINT wp_conversaciones_contacto_id_fkey FOREIGN KEY (contacto_id) REFERENCES public.wp_contactos(id),
  CONSTRAINT wp_conversaciones_numero_id_fkey FOREIGN KEY (numero_id) REFERENCES public.wp_numeros(id),
  CONSTRAINT wp_conversaciones_agente_id_fkey FOREIGN KEY (agente_id) REFERENCES public.wp_agentes(id),
  CONSTRAINT wp_conversaciones_empresa_id_fkey FOREIGN KEY (empresa_id) REFERENCES public.wp_empresa_perfil(id)
);
CREATE TABLE public.wp_crm_pagos (
  id bigint NOT NULL DEFAULT nextval('wp_crm_pagos_id_seq'::regclass),
  empresa_id bigint NOT NULL,
  servicio_id bigint NOT NULL,
  contacto_id bigint NOT NULL,
  monto numeric NOT NULL,
  moneda character varying DEFAULT 'USD'::character varying,
  fecha_pago timestamp with time zone DEFAULT now(),
  metodo_pago character varying,
  referencia text,
  estado character varying DEFAULT 'confirmado'::character varying,
  nota text,
  comprobante_url text,
  metadata jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  registrado_por bigint,
  factura_id bigint,
  CONSTRAINT wp_crm_pagos_pkey PRIMARY KEY (id),
  CONSTRAINT fk_pagos_empresa FOREIGN KEY (empresa_id) REFERENCES public.wp_empresa_perfil(id),
  CONSTRAINT fk_pagos_servicio FOREIGN KEY (servicio_id) REFERENCES public.wp_crm_servicios(id),
  CONSTRAINT fk_pagos_contacto FOREIGN KEY (contacto_id) REFERENCES public.wp_contactos(id),
  CONSTRAINT fk_pagos_registrador FOREIGN KEY (registrado_por) REFERENCES public.wp_team_humano(id),
  CONSTRAINT fk_pagos_factura FOREIGN KEY (factura_id) REFERENCES public.wp_facturas(id)
);
CREATE TABLE public.wp_crm_servicios (
  id bigint NOT NULL DEFAULT nextval('wp_crm_servicios_id_seq'::regclass),
  empresa_id bigint NOT NULL,
  contacto_id bigint NOT NULL,
  nombre_servicio text NOT NULL,
  tipo_servicio text DEFAULT 'general'::text,
  descripcion text,
  moneda character varying DEFAULT 'USD'::character varying,
  valor_total numeric NOT NULL DEFAULT 0,
  saldo_pagado numeric NOT NULL DEFAULT 0,
  saldo_pendiente numeric DEFAULT (valor_total - saldo_pagado),
  estado character varying DEFAULT 'activo'::character varying,
  fecha_inicio timestamp with time zone DEFAULT now(),
  fecha_fin timestamp with time zone,
  contrato_url text,
  metadata jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  created_by bigint,
  ultima_factura_id bigint,
  dia_compromiso_pago smallint CHECK (dia_compromiso_pago IS NULL OR dia_compromiso_pago >= 1 AND dia_compromiso_pago <= 31),
  cuota_mensual numeric CHECK (cuota_mensual IS NULL OR cuota_mensual >= 0::numeric),
  CONSTRAINT wp_crm_servicios_pkey PRIMARY KEY (id),
  CONSTRAINT fk_servicios_empresa FOREIGN KEY (empresa_id) REFERENCES public.wp_empresa_perfil(id),
  CONSTRAINT fk_servicios_contacto FOREIGN KEY (contacto_id) REFERENCES public.wp_contactos(id),
  CONSTRAINT fk_servicios_ultima_factura FOREIGN KEY (ultima_factura_id) REFERENCES public.wp_facturas(id)
);
CREATE TABLE public.wp_deep_research (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  local_job_id text NOT NULL UNIQUE,
  user_id uuid NOT NULL,
  empresa_id bigint,
  prompt text NOT NULL,
  urls ARRAY,
  schema jsonb,
  firecrawl_job_id text,
  status text NOT NULL DEFAULT 'queued'::text,
  data jsonb,
  artifact_id uuid,
  credits_used integer,
  error text,
  created_at timestamp with time zone DEFAULT now(),
  started_at timestamp with time zone,
  completed_at timestamp with time zone,
  expires_at timestamp with time zone,
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT wp_deep_research_pkey PRIMARY KEY (id),
  CONSTRAINT wp_deep_research_artifact_id_fkey FOREIGN KEY (artifact_id) REFERENCES public.artifacts(id)
);
CREATE TABLE public.wp_documentacion (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  clave text NOT NULL UNIQUE CHECK (clave ~ '^[a-z0-9_]+$'::text),
  titulo text NOT NULL,
  descripcion_corta text NOT NULL,
  descripcion_larga text,
  tipo text NOT NULL CHECK (tipo = ANY (ARRAY['tool'::text, 'table'::text, 'concept'::text, 'workflow'::text, 'api_endpoint'::text, 'function'::text])),
  ejemplos_uso jsonb DEFAULT '[]'::jsonb,
  tags jsonb DEFAULT '[]'::jsonb,
  fecha_creacion timestamp with time zone NOT NULL DEFAULT now(),
  fecha_actualizacion timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT wp_documentacion_pkey PRIMARY KEY (id)
);
CREATE TABLE public.wp_email_campanas (
  id bigint NOT NULL DEFAULT nextval('wp_email_campanas_id_seq'::regclass),
  empresa_id bigint,
  nombre text NOT NULL,
  descripcion text,
  estado text NOT NULL DEFAULT 'borrador'::text CHECK (estado = ANY (ARRAY['borrador'::text, 'activa'::text, 'pausada'::text, 'archivada'::text])),
  es_default_post_conversacion boolean DEFAULT false,
  cadencia_dias integer NOT NULL DEFAULT 2 CHECK (cadencia_dias > 0),
  total_toques integer CHECK (total_toques IS NULL OR total_toques > 0),
  instrucciones_ai text,
  metadata jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  audiencia_id bigint,
  CONSTRAINT wp_email_campanas_pkey PRIMARY KEY (id),
  CONSTRAINT wp_email_campanas_empresa_id_fkey FOREIGN KEY (empresa_id) REFERENCES public.wp_empresa_perfil(id),
  CONSTRAINT wp_email_campanas_audiencia_id_fkey FOREIGN KEY (audiencia_id) REFERENCES public.wp_marketing_audiencias(id)
);
CREATE TABLE public.wp_email_contacto_campana (
  id bigint NOT NULL DEFAULT nextval('wp_email_contacto_campana_id_seq'::regclass),
  empresa_id bigint NOT NULL,
  campana_id bigint NOT NULL,
  contacto_id bigint NOT NULL,
  estado text NOT NULL DEFAULT 'activo'::text CHECK (estado = ANY (ARRAY['activo'::text, 'completado'::text, 'cancelado'::text, 'pausado'::text, 'procesando'::text])),
  fecha_inscripcion timestamp with time zone NOT NULL DEFAULT now(),
  fecha_salida timestamp with time zone,
  motivo_salida text,
  ultimo_toque integer NOT NULL DEFAULT 0,
  proximo_envio_en timestamp with time zone,
  condiciones_entrada_capturadas jsonb,
  metadata jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT wp_email_contacto_campana_pkey PRIMARY KEY (id),
  CONSTRAINT wp_email_contacto_campana_empresa_id_fkey FOREIGN KEY (empresa_id) REFERENCES public.wp_empresa_perfil(id),
  CONSTRAINT wp_email_contacto_campana_campana_id_fkey FOREIGN KEY (campana_id) REFERENCES public.wp_email_campanas(id),
  CONSTRAINT wp_email_contacto_campana_contacto_id_fkey FOREIGN KEY (contacto_id) REFERENCES public.wp_contactos(id)
);
CREATE TABLE public.wp_email_envio (
  id bigint NOT NULL DEFAULT nextval('wp_email_envio_id_seq'::regclass),
  campana_id bigint,
  contacto_id bigint NOT NULL,
  secuencia integer NOT NULL DEFAULT 1 CHECK (secuencia > 0),
  estado text NOT NULL DEFAULT 'pendiente'::text CHECK (estado = ANY (ARRAY['borrador'::text, 'pendiente'::text, 'programado'::text, 'enviado'::text, 'abierto'::text, 'clic'::text, 'fallido'::text, 'cancelado'::text])),
  enviado_en timestamp with time zone,
  abierto_en timestamp with time zone,
  asunto text,
  cuerpo_html text,
  metadata jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  cuerpo_texto text,
  thread_id text,
  email_id text,
  remitente_team_humano bigint,
  CONSTRAINT wp_email_envio_pkey PRIMARY KEY (id),
  CONSTRAINT wp_email_envio_remitente_team_humano_fkey FOREIGN KEY (remitente_team_humano) REFERENCES public.wp_team_humano(id),
  CONSTRAINT wp_email_envio_campana_id_fkey FOREIGN KEY (campana_id) REFERENCES public.wp_email_campanas(id),
  CONSTRAINT wp_email_envio_contacto_id_fkey FOREIGN KEY (contacto_id) REFERENCES public.wp_contactos(id)
);
CREATE TABLE public.wp_email_recibido (
  id bigint NOT NULL DEFAULT nextval('wp_email_recibido_id_seq'::regclass),
  empresa_id bigint NOT NULL,
  contacto_id bigint NOT NULL,
  asunto text,
  cuerpo_html text,
  cuerpo_texto text,
  recibido_en timestamp with time zone NOT NULL DEFAULT now(),
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  thread_id text,
  email_id text,
  receptor_team_humano bigint,
  descripcion text,
  tipo text,
  contestar boolean,
  CONSTRAINT wp_email_recibido_pkey PRIMARY KEY (id),
  CONSTRAINT wp_email_recibido_empresa_id_fkey FOREIGN KEY (empresa_id) REFERENCES public.wp_empresa_perfil(id),
  CONSTRAINT wp_email_recibido_contacto_id_fkey FOREIGN KEY (contacto_id) REFERENCES public.wp_contactos(id),
  CONSTRAINT wp_email_recibido_receptor_team_humano_fkey FOREIGN KEY (receptor_team_humano) REFERENCES public.wp_team_humano(id)
);
CREATE TABLE public.wp_empresa_ads_config (
  id bigint NOT NULL DEFAULT nextval('wp_empresa_ads_config_id_seq'::regclass),
  empresa_id bigint NOT NULL,
  connector text NOT NULL,
  windsor_token text,
  campos_activos ARRAY DEFAULT ARRAY['spend'::text, 'clicks'::text, 'impressions'::text, 'conversions'::text],
  is_active boolean DEFAULT true,
  metadata jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  created_by bigint,
  CONSTRAINT wp_empresa_ads_config_pkey PRIMARY KEY (id),
  CONSTRAINT wp_empresa_ads_config_empresa_id_fkey FOREIGN KEY (empresa_id) REFERENCES public.wp_empresa_perfil(id),
  CONSTRAINT wp_empresa_ads_config_created_by_fkey FOREIGN KEY (created_by) REFERENCES public.wp_team_humano(id)
);
CREATE TABLE public.wp_empresa_embudo (
  id bigint NOT NULL DEFAULT nextval('wp_empresa_embudo_id_seq'::regclass),
  nombre_etapa character varying NOT NULL,
  orden_etapa integer NOT NULL,
  descripcion jsonb,
  empresa_id bigint NOT NULL,
  fecha_creacion timestamp with time zone NOT NULL DEFAULT now(),
  fecha_actualizacion timestamp with time zone NOT NULL DEFAULT now(),
  Respaldo json,
  configuracion_seguimiento jsonb DEFAULT '{"activo": true, "horario": {"fin": "18:00", "inicio": "08:00", "dias_permitidos": [1, 2, 3, 4, 5]}, "seguimientos": [{"numero": 1, "horas_espera": 4, "mensaje_template": "seguimiento"}, {"numero": 2, "horas_espera": 12, "mensaje_template": "seguimiento"}]}'::jsonb,
  CONSTRAINT wp_empresa_embudo_pkey PRIMARY KEY (id),
  CONSTRAINT wp_empresa_embudo_empresa_id_fkey FOREIGN KEY (empresa_id) REFERENCES public.wp_empresa_perfil(id)
);
CREATE TABLE public.wp_empresa_historial (
  id bigint NOT NULL DEFAULT nextval('wp_empresa_historial_id_seq'::regclass),
  empresa_id bigint NOT NULL,
  campo text NOT NULL,
  valor_anterior text,
  valor_nuevo text,
  usuario_id bigint,
  mensaje_commit text,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT wp_empresa_historial_pkey PRIMARY KEY (id),
  CONSTRAINT wp_empresa_historial_empresa_id_fkey FOREIGN KEY (empresa_id) REFERENCES public.wp_empresa_perfil(id),
  CONSTRAINT wp_empresa_historial_usuario_id_fkey FOREIGN KEY (usuario_id) REFERENCES public.wp_team_humano(id)
);
CREATE TABLE public.wp_empresa_perfil (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  nombre text NOT NULL,
  ciudad text,
  pais text,
  rubro text,
  informacion_empresarial text,
  preguntas_frecuentes text,
  servicios_generales text,
  embudo_ventas text,
  logo_url text,
  sitio_web text,
  telefono text,
  email text,
  direccion text,
  fecha_creacion timestamp with time zone NOT NULL DEFAULT now(),
  fecha_actualizacion timestamp with time zone NOT NULL DEFAULT now(),
  metadata jsonb DEFAULT '{}'::jsonb,
  team_slack text,
  reglas_negocio text,
  canal_comunicacion text,
  metricas_activa boolean DEFAULT false,
  timezone text,
  branding jsonb,
  activo boolean DEFAULT true,
  email_marketing boolean DEFAULT true,
  CONSTRAINT wp_empresa_perfil_pkey PRIMARY KEY (id)
);
CREATE TABLE public.wp_error_logs (
  id bigint NOT NULL DEFAULT nextval('wp_error_logs_id_seq'::regclass),
  function_name text NOT NULL,
  error_message text,
  error_stack text,
  request_body text,
  created_at timestamp with time zone DEFAULT now(),
  severity text DEFAULT 'error'::text,
  user_id uuid,
  empresa_id bigint,
  context jsonb,
  CONSTRAINT wp_error_logs_pkey PRIMARY KEY (id)
);
CREATE TABLE public.wp_etiquetas_equipo (
  id integer NOT NULL DEFAULT nextval('wp_etiquetas_equipo_id_seq'::regclass),
  empresa_id integer NOT NULL,
  nombre character varying NOT NULL,
  color character varying DEFAULT 'gray'::character varying,
  descripcion character varying,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT wp_etiquetas_equipo_pkey PRIMARY KEY (id),
  CONSTRAINT wp_etiquetas_equipo_empresa_id_fkey FOREIGN KEY (empresa_id) REFERENCES public.wp_empresa_perfil(id)
);
CREATE TABLE public.wp_evaluacion_preliminar (
  id bigint NOT NULL DEFAULT nextval('wp_evaluacion_preliminar_id_seq'::regclass),
  contacto_id bigint NOT NULL,
  codigo_criterio text NOT NULL,
  titulo text NOT NULL,
  descripcion text NOT NULL,
  ponderacion numeric NOT NULL,
  cumple boolean NOT NULL DEFAULT false,
  porcentaje_obtenido numeric DEFAULT 0,
  categoria text CHECK (categoria = ANY (ARRAY['fortaleza'::text, 'oportunidad'::text])),
  icono text,
  evidencias ARRAY,
  informacion_faltante ARRAY,
  recomendaciones ARRAY,
  observaciones text,
  nivel_confianza text CHECK (nivel_confianza = ANY (ARRAY['alto'::text, 'medio'::text, 'bajo'::text])),
  evaluacion_completa jsonb,
  fecha_evaluacion timestamp with time zone DEFAULT now(),
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  oportunidades_crecimiento jsonb,
  CONSTRAINT wp_evaluacion_preliminar_pkey PRIMARY KEY (id),
  CONSTRAINT wp_evaluacion_preliminar_contacto_id_fkey FOREIGN KEY (contacto_id) REFERENCES public.wp_contactos(id)
);
CREATE TABLE public.wp_facturas (
  id bigint NOT NULL DEFAULT nextval('wp_facturas_id_seq'::regclass),
  empresa_id bigint NOT NULL,
  contacto_id bigint NOT NULL,
  servicio_id bigint,
  pago_id bigint,
  numero_factura character varying NOT NULL,
  prefijo character varying DEFAULT 'INV'::character varying,
  secuencia integer NOT NULL,
  fecha_emision timestamp with time zone DEFAULT now(),
  fecha_vencimiento timestamp with time zone,
  cliente_nombre text NOT NULL,
  cliente_email text,
  cliente_telefono text,
  cliente_direccion text,
  cliente_documento text,
  cliente_pais character varying,
  empresa_nombre text NOT NULL,
  empresa_direccion text,
  empresa_telefono text,
  empresa_email text,
  empresa_sitio_web text,
  empresa_logo_url text,
  empresa_documento text,
  items jsonb NOT NULL DEFAULT '[]'::jsonb,
  moneda character varying DEFAULT 'USD'::character varying,
  subtotal numeric NOT NULL DEFAULT 0,
  impuestos numeric DEFAULT 0,
  descuentos numeric DEFAULT 0,
  total numeric NOT NULL DEFAULT 0 CHECK (total >= 0::numeric),
  estado character varying DEFAULT 'emitida'::character varying,
  monto_pagado numeric DEFAULT 0,
  saldo_pendiente numeric DEFAULT (total - monto_pagado),
  pdf_url text,
  notas text,
  terminos text,
  metadata jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  created_by bigint,
  CONSTRAINT wp_facturas_pkey PRIMARY KEY (id),
  CONSTRAINT fk_facturas_empresa FOREIGN KEY (empresa_id) REFERENCES public.wp_empresa_perfil(id),
  CONSTRAINT fk_facturas_contacto FOREIGN KEY (contacto_id) REFERENCES public.wp_contactos(id),
  CONSTRAINT fk_facturas_servicio FOREIGN KEY (servicio_id) REFERENCES public.wp_crm_servicios(id),
  CONSTRAINT fk_facturas_pago FOREIGN KEY (pago_id) REFERENCES public.wp_crm_pagos(id)
);
CREATE TABLE public.wp_finanzas (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  empresa_id bigint,
  contacto_id bigint,
  fecha timestamp with time zone DEFAULT now(),
  tipo USER-DEFINED NOT NULL,
  concepto text NOT NULL,
  monto numeric NOT NULL,
  estado USER-DEFINED DEFAULT 'activo'::finanza_estado,
  categoria character varying,
  subcategoria character varying,
  descripcion text,
  fecha_vencimiento date,
  referencia character varying,
  metodo_pago character varying,
  stripe_session_id character varying,
  stripe_subscription_id character varying,
  notas text,
  metadata jsonb,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT wp_finanzas_pkey PRIMARY KEY (id),
  CONSTRAINT wp_finanzas_empresa_id_fkey FOREIGN KEY (empresa_id) REFERENCES public.wp_empresa_perfil(id),
  CONSTRAINT wp_finanzas_contacto_id_fkey FOREIGN KEY (contacto_id) REFERENCES public.wp_contactos(id)
);
CREATE TABLE public.wp_follow_up (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  tipo text,
  canal text,
  intento text,
  mensaje text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT wp_follow_up_pkey PRIMARY KEY (id)
);
CREATE TABLE public.wp_marketing_audiencia_contacto (
  id bigint NOT NULL DEFAULT nextval('wp_marketing_audiencia_contacto_id_seq'::regclass),
  audiencia_id bigint NOT NULL,
  contacto_id bigint NOT NULL,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT wp_marketing_audiencia_contacto_pkey PRIMARY KEY (id),
  CONSTRAINT wp_marketing_audiencia_contacto_audiencia_id_fkey FOREIGN KEY (audiencia_id) REFERENCES public.wp_marketing_audiencias(id),
  CONSTRAINT wp_marketing_audiencia_contacto_contacto_id_fkey FOREIGN KEY (contacto_id) REFERENCES public.wp_contactos(id)
);
CREATE TABLE public.wp_marketing_audiencias (
  id bigint NOT NULL DEFAULT nextval('wp_marketing_audiencias_id_seq'::regclass),
  empresa_id bigint NOT NULL,
  nombre text NOT NULL,
  descripcion text,
  tipo text NOT NULL DEFAULT 'estatica'::text,
  filtros_json jsonb DEFAULT '{}'::jsonb,
  metadata jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT wp_marketing_audiencias_pkey PRIMARY KEY (id),
  CONSTRAINT wp_marketing_audiencias_empresa_id_fkey FOREIGN KEY (empresa_id) REFERENCES public.wp_empresa_perfil(id)
);
CREATE TABLE public.wp_mcp_tools_catalog (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  nombre_tool text NOT NULL UNIQUE,
  descripcion text NOT NULL,
  schema jsonb NOT NULL,
  categoria text,
  endpoint text,
  version text DEFAULT '1.0.0'::text,
  activo boolean NOT NULL DEFAULT true,
  metadata jsonb,
  empresa_id bigint,
  fecha_creacion timestamp with time zone NOT NULL DEFAULT now(),
  fecha_actualizacion timestamp with time zone NOT NULL DEFAULT now(),
  permisos jsonb DEFAULT '{"get": true, "post": false}'::jsonb,
  secreto text,
  CONSTRAINT wp_mcp_tools_catalog_pkey PRIMARY KEY (id),
  CONSTRAINT wp_mcp_tools_catalog_empresa_id_fkey FOREIGN KEY (empresa_id) REFERENCES public.wp_empresa_perfil(id)
);
CREATE TABLE public.wp_mensajes (
  id bigint NOT NULL DEFAULT nextval('wp_mensajes_id_seq'::regclass),
  conversacion_id bigint NOT NULL,
  timestamp timestamp with time zone NOT NULL DEFAULT now(),
  contenido text NOT NULL,
  tipo text NOT NULL DEFAULT 'texto'::text CHECK (tipo = ANY (ARRAY['texto'::text, 'multimedia'::text])),
  remitente text NOT NULL,
  status text DEFAULT 'buffer'::text,
  metadata jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  modelo_llm text DEFAULT ''::text,
  uso_herramientas jsonb,
  url_ejecucion text,
  empresa_id bigint,
  id_externo text,
  CONSTRAINT wp_mensajes_pkey PRIMARY KEY (id),
  CONSTRAINT wp_mensajes_conversacion_id_fkey FOREIGN KEY (conversacion_id) REFERENCES public.wp_conversaciones(id)
);
CREATE TABLE public.wp_metodos_pago (
  id bigint NOT NULL DEFAULT nextval('wp_metodos_pago_id_seq'::regclass),
  empresa_id bigint NOT NULL,
  codigo character varying NOT NULL,
  nombre character varying NOT NULL,
  descripcion text,
  icono character varying DEFAULT 'wallet'::character varying,
  is_active boolean DEFAULT true,
  orden integer DEFAULT 0,
  metadata jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  created_by bigint,
  CONSTRAINT wp_metodos_pago_pkey PRIMARY KEY (id),
  CONSTRAINT fk_metodos_pago_empresa FOREIGN KEY (empresa_id) REFERENCES public.wp_empresa_perfil(id)
);
CREATE TABLE public.wp_module_usage_daily (
  id bigint NOT NULL DEFAULT nextval('wp_module_usage_daily_id_seq'::regclass),
  empresa_id bigint,
  date date NOT NULL,
  module character varying NOT NULL,
  unique_users integer DEFAULT 0,
  total_views integer DEFAULT 0,
  total_actions integer DEFAULT 0,
  avg_time_seconds integer DEFAULT 0,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT wp_module_usage_daily_pkey PRIMARY KEY (id),
  CONSTRAINT wp_module_usage_daily_empresa_id_fkey FOREIGN KEY (empresa_id) REFERENCES public.wp_empresa_perfil(id)
);
CREATE TABLE public.wp_multimedia (
  id bigint NOT NULL DEFAULT nextval('wp_multimedia_id_seq'::regclass),
  archivo_url text NOT NULL,
  tipo text NOT NULL CHECK (tipo = ANY (ARRAY['imagen'::text, 'audio'::text, 'video'::text, 'documento'::text])),
  nombre_archivo text,
  tamaño bigint,
  metadata jsonb DEFAULT '{}'::jsonb,
  hash text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  contacto_id bigint,
  contenido jsonb,
  url_carpeta text,
  estado text,
  empresa_id bigint,
  seccion text,
  CONSTRAINT wp_multimedia_pkey PRIMARY KEY (id),
  CONSTRAINT wp_multimedia_contacto_id_fkey FOREIGN KEY (contacto_id) REFERENCES public.wp_contactos(id)
);
CREATE TABLE public.wp_notificaciones (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  tipo text NOT NULL,
  titulo text NOT NULL,
  mensaje text NOT NULL,
  contacto_id bigint,
  metadata jsonb DEFAULT '{}'::jsonb,
  team_humano bigint,
  CONSTRAINT wp_notificaciones_pkey PRIMARY KEY (id),
  CONSTRAINT wp_notificaciones_contacto_id_fkey1 FOREIGN KEY (contacto_id) REFERENCES public.wp_contactos(id),
  CONSTRAINT wp_notificaciones_team_humano_fkey FOREIGN KEY (team_humano) REFERENCES public.wp_team_humano(id)
);
CREATE TABLE public.wp_notificaciones_team (
  id bigint NOT NULL DEFAULT nextval('wp_notificaciones_id_seq'::regclass),
  tipo text NOT NULL,
  contacto_id bigint,
  mensaje text NOT NULL,
  fecha_envio timestamp with time zone NOT NULL DEFAULT now(),
  estado text NOT NULL DEFAULT 'pendiente'::text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  respuesta text,
  fecha_respuesta timestamp with time zone,
  empresa_id bigint,
  asesor_id bigint,
  agente_id bigint,
  origen text DEFAULT ''::text,
  requiere_respuesta boolean DEFAULT true,
  visto boolean DEFAULT false,
  metadata jsonb,
  archivado boolean DEFAULT false,
  CONSTRAINT wp_notificaciones_team_pkey PRIMARY KEY (id),
  CONSTRAINT wp_notificaciones_team_contacto_id_fkey FOREIGN KEY (contacto_id) REFERENCES public.wp_contactos(id),
  CONSTRAINT wp_notificaciones_contacto_id_fkey FOREIGN KEY (contacto_id) REFERENCES public.wp_contactos(id),
  CONSTRAINT wp_notificaciones_team_empresa_id_fkey FOREIGN KEY (empresa_id) REFERENCES public.wp_empresa_perfil(id),
  CONSTRAINT wp_notificaciones_team_asesor_id_fkey FOREIGN KEY (asesor_id) REFERENCES public.wp_team_humano(id),
  CONSTRAINT wp_notificaciones_team_agente_id_fkey FOREIGN KEY (agente_id) REFERENCES public.wp_agentes(id)
);
CREATE TABLE public.wp_numeros (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  telefono text UNIQUE,
  nombre text,
  activo boolean DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  empresa_id bigint,
  agente_id bigint,
  canal text,
  waha_session text,
  evolution_instance text,
  elevenlabs_id_voz text,
  timezone text,
  link_conectar_canal text,
  id_kapso text,
  agente_id_ab bigint,
  ab_test_activo boolean DEFAULT false,
  CONSTRAINT wp_numeros_pkey PRIMARY KEY (id),
  CONSTRAINT wp_numeros_empresa_id_fkey FOREIGN KEY (empresa_id) REFERENCES public.wp_empresa_perfil(id),
  CONSTRAINT wp_numeros_agente_id_fkey FOREIGN KEY (agente_id) REFERENCES public.wp_agentes(id),
  CONSTRAINT wp_numeros_agente_id_ab_fkey FOREIGN KEY (agente_id_ab) REFERENCES public.wp_agentes(id)
);
CREATE TABLE public.wp_numeros_horarios (
  id bigint NOT NULL DEFAULT nextval('wp_numeros_horarios_id_seq'::regclass),
  numero_id bigint NOT NULL UNIQUE,
  empresa_id bigint NOT NULL,
  hora_activar time without time zone NOT NULL DEFAULT '17:00:00'::time without time zone,
  hora_desactivar time without time zone NOT NULL DEFAULT '06:00:00'::time without time zone,
  dias_desactivar ARRAY NOT NULL DEFAULT '{1,2,3,4,5}'::integer[],
  dias_siempre_activo ARRAY NOT NULL DEFAULT '{6,0}'::integer[],
  timezone text NOT NULL DEFAULT 'America/New_York'::text,
  habilitado boolean NOT NULL DEFAULT true,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT wp_numeros_horarios_pkey PRIMARY KEY (id),
  CONSTRAINT wp_numeros_horarios_numero_id_fkey FOREIGN KEY (numero_id) REFERENCES public.wp_numeros(id),
  CONSTRAINT wp_numeros_horarios_empresa_id_fkey FOREIGN KEY (empresa_id) REFERENCES public.wp_empresa_perfil(id)
);
CREATE TABLE public.wp_plan_consumo (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  id_plan uuid,
  id_empresa bigint,
  caracteristica_id bigint,
  cantidad_consumida numeric,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  consumo_extra numeric DEFAULT 0,
  CONSTRAINT wp_plan_consumo_pkey PRIMARY KEY (id),
  CONSTRAINT wp_plan_consumo_id_plan_fkey FOREIGN KEY (id_plan) REFERENCES public.wp_planes_suscripcion(id),
  CONSTRAINT wp_plan_consumo_caracteristica_id_fkey FOREIGN KEY (caracteristica_id) REFERENCES public.wp_planes_caracteristicas(id),
  CONSTRAINT wp_plan_consumo_id_empresa_fkey FOREIGN KEY (id_empresa) REFERENCES public.wp_empresa_perfil(id)
);
CREATE TABLE public.wp_planes_caracteristicas (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  caracteristica text,
  plan_id uuid,
  descripccion text,
  cantidad numeric,
  CONSTRAINT wp_planes_caracteristicas_pkey PRIMARY KEY (id),
  CONSTRAINT wp_planes_caracteristicas_plan_id_fkey FOREIGN KEY (plan_id) REFERENCES public.wp_planes_suscripcion(id)
);
CREATE TABLE public.wp_planes_suscripcion (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  nombre character varying NOT NULL,
  descripcion text,
  precio numeric NOT NULL,
  moneda character varying DEFAULT 'USD'::character varying,
  periodo USER-DEFINED DEFAULT 'monthly'::plan_periodo,
  stripe_price_id character varying,
  activo boolean DEFAULT true,
  dias_prueba integer DEFAULT 0,
  duracion_dias integer,
  metadata jsonb,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT wp_planes_suscripcion_pkey PRIMARY KEY (id)
);
CREATE TABLE public.wp_proyectos (
  id bigint NOT NULL DEFAULT nextval('wp_proyectos_id_seq'::regclass),
  empresa_id integer NOT NULL,
  nombre character varying NOT NULL,
  descripcion text,
  estado character varying DEFAULT 'activo'::character varying CHECK (estado::text = ANY (ARRAY['activo'::character varying, 'archivado'::character varying, 'completado'::character varying]::text[])),
  color character varying DEFAULT 'blue'::character varying,
  icono character varying DEFAULT 'folder'::character varying,
  orden smallint DEFAULT 0,
  config jsonb DEFAULT '{"vista_default": "lista", "columnas_kanban": ["pendiente", "en_progreso", "completada"]}'::jsonb,
  creado_por integer NOT NULL,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  contacto_id integer,
  servicio_id integer,
  presupuesto numeric,
  gasto_actual numeric DEFAULT 0,
  moneda character varying DEFAULT 'USD'::character varying,
  fecha_inicio date,
  fecha_fin_estimada date,
  fecha_fin_real date,
  CONSTRAINT wp_proyectos_pkey PRIMARY KEY (id),
  CONSTRAINT wp_proyectos_empresa_id_fkey FOREIGN KEY (empresa_id) REFERENCES public.wp_empresa_perfil(id),
  CONSTRAINT wp_proyectos_creado_por_fkey FOREIGN KEY (creado_por) REFERENCES public.wp_team_humano(id),
  CONSTRAINT wp_proyectos_contacto_id_fkey FOREIGN KEY (contacto_id) REFERENCES public.wp_contactos(id),
  CONSTRAINT wp_proyectos_servicio_id_fkey FOREIGN KEY (servicio_id) REFERENCES public.wp_crm_servicios(id)
);
CREATE TABLE public.wp_proyectos_costos (
  id bigint NOT NULL DEFAULT nextval('wp_proyectos_costos_id_seq'::regclass),
  proyecto_id integer NOT NULL,
  concepto character varying NOT NULL,
  categoria character varying DEFAULT 'general'::character varying CHECK (categoria::text = ANY (ARRAY['personal'::character varying, 'licencias'::character varying, 'infraestructura'::character varying, 'servicios'::character varying, 'general'::character varying]::text[])),
  monto numeric NOT NULL,
  moneda character varying DEFAULT 'USD'::character varying,
  tarea_id integer,
  fecha_costo date NOT NULL DEFAULT CURRENT_DATE,
  registrado_por integer,
  comprobante_url text,
  notas text,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT wp_proyectos_costos_pkey PRIMARY KEY (id),
  CONSTRAINT wp_proyectos_costos_proyecto_id_fkey FOREIGN KEY (proyecto_id) REFERENCES public.wp_proyectos(id),
  CONSTRAINT wp_proyectos_costos_tarea_id_fkey FOREIGN KEY (tarea_id) REFERENCES public.wp_tareas(id),
  CONSTRAINT wp_proyectos_costos_registrado_por_fkey FOREIGN KEY (registrado_por) REFERENCES public.wp_team_humano(id)
);
CREATE TABLE public.wp_recordatorios (
  id bigint NOT NULL DEFAULT nextval('wp_recordatorios_id_seq'::regclass),
  contacto_id bigint NOT NULL,
  conversacion_id bigint,
  titulo text NOT NULL,
  descripcion text,
  fecha_hora timestamp with time zone NOT NULL,
  repeticion text DEFAULT 'no'::text CHECK (repeticion = ANY (ARRAY['no'::text, 'diaria'::text, 'semanal'::text, 'mensual'::text])),
  estado text NOT NULL DEFAULT 'pendiente'::text CHECK (estado = ANY (ARRAY['pendiente'::text, 'completado'::text, 'cancelado'::text])),
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT wp_recordatorios_pkey PRIMARY KEY (id),
  CONSTRAINT wp_recordatorios_contacto_id_fkey FOREIGN KEY (contacto_id) REFERENCES public.wp_contactos(id),
  CONSTRAINT wp_recordatorios_conversacion_id_fkey FOREIGN KEY (conversacion_id) REFERENCES public.wp_conversaciones(id)
);
CREATE TABLE public.wp_suscripcion_historial (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  suscripcion_id uuid,
  plan_id uuid,
  cambio_fecha timestamp with time zone DEFAULT now(),
  motivo text,
  precio_aplicado numeric,
  CONSTRAINT wp_suscripcion_historial_pkey PRIMARY KEY (id),
  CONSTRAINT wp_suscripcion_historial_plan_id_fkey FOREIGN KEY (plan_id) REFERENCES public.wp_planes_suscripcion(id),
  CONSTRAINT wp_suscripcion_historial_suscripcion_id_fkey FOREIGN KEY (suscripcion_id) REFERENCES public.wp_suscripciones(id)
);
CREATE TABLE public.wp_suscripciones (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  empresa_id bigint,
  contacto_id bigint,
  plan_id uuid,
  stripe_id_suscripcion character varying UNIQUE,
  stripe_id_cliente character varying,
  estado USER-DEFINED DEFAULT 'active'::suscripcion_estado,
  fecha_inicio timestamp with time zone DEFAULT now(),
  fecha_fin timestamp with time zone,
  proxima_facturacion timestamp with time zone,
  cancel_at timestamp with time zone,
  precio_actual numeric,
  metadata jsonb,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT wp_suscripciones_pkey PRIMARY KEY (id),
  CONSTRAINT wp_suscripciones_empresa_id_fkey FOREIGN KEY (empresa_id) REFERENCES public.wp_empresa_perfil(id),
  CONSTRAINT wp_suscripciones_contacto_id_fkey FOREIGN KEY (contacto_id) REFERENCES public.wp_contactos(id),
  CONSTRAINT wp_suscripciones_plan_id_fkey FOREIGN KEY (plan_id) REFERENCES public.wp_planes_suscripcion(id)
);
CREATE TABLE public.wp_system_alerts (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  type text NOT NULL,
  severity text NOT NULL DEFAULT 'medium'::text,
  title text NOT NULL,
  message text,
  context jsonb,
  empresa_id bigint,
  user_id uuid,
  dismissed_at timestamp with time zone,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT wp_system_alerts_pkey PRIMARY KEY (id),
  CONSTRAINT wp_system_alerts_empresa_id_fkey FOREIGN KEY (empresa_id) REFERENCES public.wp_empresa_perfil(id)
);
CREATE TABLE public.wp_tareas (
  id bigint NOT NULL DEFAULT nextval('wp_tareas_id_seq'::regclass),
  titulo character varying NOT NULL,
  descripcion text,
  estado character varying DEFAULT 'pendiente'::character varying CHECK (estado::text = ANY (ARRAY['pendiente'::character varying, 'en_progreso'::character varying, 'completada'::character varying, 'cancelada'::character varying]::text[])),
  prioridad smallint DEFAULT 2 CHECK (prioridad >= 1 AND prioridad <= 4),
  contacto_id integer,
  cita_id integer,
  conversacion_id integer,
  empresa_id integer NOT NULL,
  asignado_a integer,
  creado_por integer NOT NULL,
  fecha_vencimiento timestamp with time zone,
  fecha_completada timestamp with time zone,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  metadata jsonb DEFAULT '{}'::jsonb,
  proyecto_id integer,
  descripcion_md text,
  portada_url text,
  tiempo_estimado_min integer,
  tiempo_real_min integer DEFAULT 0,
  costo_estimado numeric,
  costo_real numeric DEFAULT 0,
  moneda character varying DEFAULT 'USD'::character varying,
  CONSTRAINT wp_tareas_pkey PRIMARY KEY (id),
  CONSTRAINT wp_tareas_contacto_id_fkey FOREIGN KEY (contacto_id) REFERENCES public.wp_contactos(id),
  CONSTRAINT wp_tareas_cita_id_fkey FOREIGN KEY (cita_id) REFERENCES public.wp_citas(id),
  CONSTRAINT wp_tareas_conversacion_id_fkey FOREIGN KEY (conversacion_id) REFERENCES public.wp_conversaciones(id),
  CONSTRAINT wp_tareas_empresa_id_fkey FOREIGN KEY (empresa_id) REFERENCES public.wp_empresa_perfil(id),
  CONSTRAINT wp_tareas_asignado_a_fkey FOREIGN KEY (asignado_a) REFERENCES public.wp_team_humano(id),
  CONSTRAINT wp_tareas_creado_por_fkey FOREIGN KEY (creado_por) REFERENCES public.wp_team_humano(id),
  CONSTRAINT wp_tareas_proyecto_id_fkey FOREIGN KEY (proyecto_id) REFERENCES public.wp_proyectos(id)
);
CREATE TABLE public.wp_tareas_asignados (
  tarea_id integer NOT NULL,
  team_humano_id integer NOT NULL,
  rol character varying DEFAULT 'responsable'::character varying CHECK (rol::text = ANY (ARRAY['responsable'::character varying, 'colaborador'::character varying, 'revisor'::character varying]::text[])),
  asignado_por integer,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT wp_tareas_asignados_pkey PRIMARY KEY (tarea_id, team_humano_id),
  CONSTRAINT wp_tareas_asignados_tarea_id_fkey FOREIGN KEY (tarea_id) REFERENCES public.wp_tareas(id),
  CONSTRAINT wp_tareas_asignados_team_humano_id_fkey FOREIGN KEY (team_humano_id) REFERENCES public.wp_team_humano(id),
  CONSTRAINT wp_tareas_asignados_asignado_por_fkey FOREIGN KEY (asignado_por) REFERENCES public.wp_team_humano(id)
);
CREATE TABLE public.wp_tareas_comentarios (
  id bigint NOT NULL DEFAULT nextval('wp_tareas_comentarios_id_seq'::regclass),
  tarea_id integer NOT NULL,
  autor_id integer,
  contenido text NOT NULL,
  tipo character varying DEFAULT 'comentario'::character varying CHECK (tipo::text = ANY (ARRAY['comentario'::character varying, 'sistema'::character varying, 'mencion'::character varying]::text[])),
  metadata jsonb DEFAULT '{}'::jsonb,
  editado boolean DEFAULT false,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT wp_tareas_comentarios_pkey PRIMARY KEY (id),
  CONSTRAINT wp_tareas_comentarios_tarea_id_fkey FOREIGN KEY (tarea_id) REFERENCES public.wp_tareas(id),
  CONSTRAINT wp_tareas_comentarios_autor_id_fkey FOREIGN KEY (autor_id) REFERENCES public.wp_team_humano(id)
);
CREATE TABLE public.wp_tareas_etiquetas (
  tarea_id integer NOT NULL,
  etiqueta_id integer NOT NULL,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT wp_tareas_etiquetas_pkey PRIMARY KEY (tarea_id, etiqueta_id),
  CONSTRAINT wp_tareas_etiquetas_tarea_id_fkey FOREIGN KEY (tarea_id) REFERENCES public.wp_tareas(id),
  CONSTRAINT wp_tareas_etiquetas_etiqueta_id_fkey FOREIGN KEY (etiqueta_id) REFERENCES public.wp_etiquetas_equipo(id)
);
CREATE TABLE public.wp_tareas_historial (
  id bigint NOT NULL DEFAULT nextval('wp_tareas_historial_id_seq'::regclass),
  tarea_id integer NOT NULL,
  accion character varying NOT NULL,
  campo_modificado character varying,
  valor_anterior text,
  valor_nuevo text,
  autor_id integer,
  metadata jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT wp_tareas_historial_pkey PRIMARY KEY (id),
  CONSTRAINT wp_tareas_historial_tarea_id_fkey FOREIGN KEY (tarea_id) REFERENCES public.wp_tareas(id),
  CONSTRAINT wp_tareas_historial_autor_id_fkey FOREIGN KEY (autor_id) REFERENCES public.wp_team_humano(id)
);
CREATE TABLE public.wp_tareas_items (
  id bigint NOT NULL DEFAULT nextval('wp_tareas_items_id_seq'::regclass),
  tarea_id integer NOT NULL,
  texto character varying NOT NULL,
  orden smallint NOT NULL DEFAULT 0,
  completado boolean DEFAULT false,
  completado_por integer,
  completado_at timestamp with time zone,
  created_at timestamp with time zone DEFAULT now(),
  asignado_a integer,
  etiqueta_id integer,
  CONSTRAINT wp_tareas_items_pkey PRIMARY KEY (id),
  CONSTRAINT wp_tareas_items_tarea_id_fkey FOREIGN KEY (tarea_id) REFERENCES public.wp_tareas(id),
  CONSTRAINT wp_tareas_items_completado_por_fkey FOREIGN KEY (completado_por) REFERENCES public.wp_team_humano(id),
  CONSTRAINT wp_tareas_items_asignado_a_fkey FOREIGN KEY (asignado_a) REFERENCES public.wp_team_humano(id),
  CONSTRAINT wp_tareas_items_etiqueta_id_fkey FOREIGN KEY (etiqueta_id) REFERENCES public.wp_etiquetas_equipo(id)
);
CREATE TABLE public.wp_tareas_media (
  id bigint NOT NULL DEFAULT nextval('wp_tareas_media_id_seq'::regclass),
  tarea_id integer NOT NULL,
  nombre_archivo character varying NOT NULL,
  tipo_mime character varying NOT NULL,
  tamaño_bytes bigint NOT NULL,
  storage_path text NOT NULL,
  url_publica text,
  descripcion character varying,
  es_portada boolean DEFAULT false,
  subido_por integer,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT wp_tareas_media_pkey PRIMARY KEY (id),
  CONSTRAINT wp_tareas_media_tarea_id_fkey FOREIGN KEY (tarea_id) REFERENCES public.wp_tareas(id),
  CONSTRAINT wp_tareas_media_subido_por_fkey FOREIGN KEY (subido_por) REFERENCES public.wp_team_humano(id)
);
CREATE TABLE public.wp_tareas_reacciones (
  comentario_id integer NOT NULL,
  usuario_id integer NOT NULL,
  emoji character varying NOT NULL,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT wp_tareas_reacciones_pkey PRIMARY KEY (comentario_id, usuario_id, emoji),
  CONSTRAINT wp_tareas_reacciones_comentario_id_fkey FOREIGN KEY (comentario_id) REFERENCES public.wp_tareas_comentarios(id),
  CONSTRAINT wp_tareas_reacciones_usuario_id_fkey FOREIGN KEY (usuario_id) REFERENCES public.wp_team_humano(id)
);
CREATE TABLE public.wp_team_humano (
  id bigint GENERATED ALWAYS AS IDENTITY NOT NULL,
  empresa_id bigint NOT NULL,
  nombre text NOT NULL,
  apellido text,
  email text NOT NULL UNIQUE,
  telefono text,
  rol text NOT NULL,
  especialidad text,
  is_active boolean DEFAULT true,
  prioridad text,
  metadata jsonb DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  timezone text DEFAULT 'America/Bogota'::text,
  disponibilidad jsonb DEFAULT jsonb_build_object('horarios_normales', jsonb_build_object('lunes', jsonb_build_array(jsonb_build_object('inicio', '09:00', 'fin', '15:00')), 'martes', jsonb_build_array(jsonb_build_object('inicio', '09:00', 'fin', '15:00')), 'miercoles', jsonb_build_array(jsonb_build_object('inicio', '09:00', 'fin', '15:00')), 'jueves', jsonb_build_array(jsonb_build_object('inicio', '09:00', 'fin', '15:00')), 'viernes', jsonb_build_array(jsonb_build_object('inicio', '09:00', 'fin', '15:00'))), 'horarios_especiales', jsonb_build_array(), 'vacaciones', jsonb_build_array(), 'buffer_entre_citas', 15, 'duracion_maxima_cita', 240, 'permite_fines_semana', false),
  calendly text DEFAULT 'No usa enlace'::text,
  grupo_whatsapp text,
  webinar text,
  slack_id text,
  ultima_asignacion timestamp with time zone,
  deleted timestamp with time zone,
  grant_id text,
  id_contacto bigint,
  duracion_cita_minutos integer DEFAULT 30,
  acepta_citas boolean DEFAULT false,
  notetaker boolean DEFAULT true,
  auth_uid uuid UNIQUE,
  analisis_asesor text,
  role_id bigint DEFAULT '3'::bigint,
  enterprise_id bigint NOT NULL,
  multimedia text,
  temporal_nylas text,
  CONSTRAINT wp_team_humano_pkey PRIMARY KEY (id),
  CONSTRAINT wp_team_humano_role_id_fkey FOREIGN KEY (role_id) REFERENCES public.system_roles(id),
  CONSTRAINT wp_team_humano_enterprise_id_fkey FOREIGN KEY (enterprise_id) REFERENCES public.wp_empresa_perfil(id),
  CONSTRAINT wp_team_humano_id_contacto_fkey FOREIGN KEY (id_contacto) REFERENCES public.wp_contactos(id),
  CONSTRAINT wp_team_humano_empresa_id_fkey FOREIGN KEY (empresa_id) REFERENCES public.wp_empresa_perfil(id)
);
CREATE TABLE public.wp_team_invitations (
  id bigint NOT NULL DEFAULT nextval('wp_team_invitations_id_seq'::regclass),
  token uuid NOT NULL DEFAULT gen_random_uuid() UNIQUE,
  email character varying NOT NULL,
  rol character varying NOT NULL DEFAULT 'asesor'::character varying,
  role_id integer NOT NULL DEFAULT 3,
  empresa_id integer NOT NULL,
  invited_by integer,
  status character varying NOT NULL DEFAULT 'pending'::character varying CHECK (status::text = ANY (ARRAY['pending'::character varying, 'accepted'::character varying, 'expired'::character varying, 'cancelled'::character varying]::text[])),
  created_at timestamp with time zone DEFAULT now(),
  expires_at timestamp with time zone DEFAULT (now() + '7 days'::interval),
  accepted_at timestamp with time zone,
  team_member_id integer,
  metadata jsonb DEFAULT '{}'::jsonb,
  CONSTRAINT wp_team_invitations_pkey PRIMARY KEY (id),
  CONSTRAINT wp_team_invitations_empresa_id_fkey FOREIGN KEY (empresa_id) REFERENCES public.wp_empresa_perfil(id),
  CONSTRAINT wp_team_invitations_invited_by_fkey FOREIGN KEY (invited_by) REFERENCES public.wp_team_humano(id),
  CONSTRAINT wp_team_invitations_team_member_id_fkey FOREIGN KEY (team_member_id) REFERENCES public.wp_team_humano(id)
);
CREATE TABLE public.wp_user_engagement (
  id bigint NOT NULL DEFAULT nextval('wp_user_engagement_id_seq'::regclass),
  user_id uuid NOT NULL,
  team_humano_id bigint,
  empresa_id bigint,
  event_type character varying NOT NULL CHECK (event_type::text = ANY (ARRAY['page_view'::character varying, 'action'::character varying, 'feature_use'::character varying, 'session_start'::character varying, 'session_end'::character varying]::text[])),
  event_name character varying NOT NULL,
  module character varying NOT NULL,
  sub_module character varying,
  metadata jsonb DEFAULT '{}'::jsonb,
  session_id character varying,
  device_type character varying,
  user_agent text,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT wp_user_engagement_pkey PRIMARY KEY (id),
  CONSTRAINT wp_user_engagement_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id),
  CONSTRAINT wp_user_engagement_team_humano_id_fkey FOREIGN KEY (team_humano_id) REFERENCES public.wp_team_humano(id),
  CONSTRAINT wp_user_engagement_empresa_id_fkey FOREIGN KEY (empresa_id) REFERENCES public.wp_empresa_perfil(id)
);
CREATE TABLE public.wp_user_engagement_daily (
  id bigint NOT NULL DEFAULT nextval('wp_user_engagement_daily_id_seq'::regclass),
  user_id uuid NOT NULL,
  empresa_id bigint,
  date date NOT NULL,
  total_events integer DEFAULT 0,
  total_page_views integer DEFAULT 0,
  total_actions integer DEFAULT 0,
  session_count integer DEFAULT 0,
  total_duration_seconds integer DEFAULT 0,
  modules_used ARRAY DEFAULT '{}'::text[],
  features_used ARRAY DEFAULT '{}'::text[],
  first_activity_at timestamp with time zone,
  last_activity_at timestamp with time zone,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT wp_user_engagement_daily_pkey PRIMARY KEY (id),
  CONSTRAINT wp_user_engagement_daily_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id),
  CONSTRAINT wp_user_engagement_daily_empresa_id_fkey FOREIGN KEY (empresa_id) REFERENCES public.wp_empresa_perfil(id)
);
CREATE TABLE public.wp_videos (
  id bigint NOT NULL DEFAULT nextval('wp_videos_id_seq'::regclass),
  nombre_video text,
  fecha date,
  link text,
  analisis_video text,
  empresa_id bigint NOT NULL,
  cita_id bigint,
  nombre_asesor text,
  asesor_id bigint,
  cita_externa_id text,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT wp_videos_pkey PRIMARY KEY (id),
  CONSTRAINT fk_video_empresa FOREIGN KEY (empresa_id) REFERENCES public.wp_empresa_perfil(id),
  CONSTRAINT fk_video_cita FOREIGN KEY (cita_id) REFERENCES public.wp_citas(id),
  CONSTRAINT fk_video_asesor FOREIGN KEY (asesor_id) REFERENCES public.wp_team_humano(id)
);
CREATE TABLE public.wp_whatsapp_template_envios (
  id bigint NOT NULL DEFAULT nextval('wp_whatsapp_template_envios_id_seq'::regclass),
  empresa_id bigint NOT NULL,
  numero_id bigint NOT NULL,
  template_id bigint,
  conversacion_id bigint,
  mensaje_id bigint,
  contacto_id bigint,
  enviado_por bigint,
  provider text NOT NULL DEFAULT 'kapso'::text CHECK (provider = ANY (ARRAY['kapso'::text, 'meta'::text, 'manual'::text])),
  provider_message_id text,
  provider_template_id text,
  template_name text NOT NULL,
  language_code text NOT NULL,
  category text CHECK (category IS NULL OR (category = ANY (ARRAY['marketing'::text, 'utility'::text, 'authentication'::text]))),
  telefono_destino text NOT NULL,
  payload jsonb NOT NULL DEFAULT '{}'::jsonb,
  parametros_resueltos jsonb NOT NULL DEFAULT '[]'::jsonb,
  rendered_body text,
  estado text NOT NULL DEFAULT 'queued'::text CHECK (estado = ANY (ARRAY['queued'::text, 'accepted'::text, 'sent'::text, 'delivered'::text, 'read'::text, 'failed'::text, 'rejected'::text, 'cancelled'::text])),
  error_code text,
  error_message text,
  sent_at timestamp with time zone,
  delivered_at timestamp with time zone,
  read_at timestamp with time zone,
  failed_at timestamp with time zone,
  metadata jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  meta_category text,
  clasificacion_interna text,
  CONSTRAINT wp_whatsapp_template_envios_pkey PRIMARY KEY (id),
  CONSTRAINT wp_whatsapp_template_envios_empresa_id_fkey FOREIGN KEY (empresa_id) REFERENCES public.wp_empresa_perfil(id),
  CONSTRAINT wp_whatsapp_template_envios_numero_id_fkey FOREIGN KEY (numero_id) REFERENCES public.wp_numeros(id),
  CONSTRAINT wp_whatsapp_template_envios_template_id_fkey FOREIGN KEY (template_id) REFERENCES public.wp_whatsapp_templates(id),
  CONSTRAINT wp_whatsapp_template_envios_conversacion_id_fkey FOREIGN KEY (conversacion_id) REFERENCES public.wp_conversaciones(id),
  CONSTRAINT wp_whatsapp_template_envios_mensaje_id_fkey FOREIGN KEY (mensaje_id) REFERENCES public.wp_mensajes(id),
  CONSTRAINT wp_whatsapp_template_envios_contacto_id_fkey FOREIGN KEY (contacto_id) REFERENCES public.wp_contactos(id),
  CONSTRAINT wp_whatsapp_template_envios_enviado_por_fkey FOREIGN KEY (enviado_por) REFERENCES public.wp_team_humano(id)
);
CREATE TABLE public.wp_whatsapp_templates (
  id bigint NOT NULL DEFAULT nextval('wp_whatsapp_templates_id_seq'::regclass),
  empresa_id bigint NOT NULL,
  numero_id bigint NOT NULL,
  provider text NOT NULL DEFAULT 'kapso'::text CHECK (provider = ANY (ARRAY['kapso'::text, 'meta'::text, 'manual'::text])),
  provider_phone_id text,
  provider_template_id text,
  business_account_id text,
  template_name text NOT NULL,
  language_code text NOT NULL,
  category text NOT NULL CHECK (category = ANY (ARRAY['marketing'::text, 'utility'::text, 'authentication'::text])),
  status text NOT NULL DEFAULT 'pending'::text CHECK (status = ANY (ARRAY['draft'::text, 'pending'::text, 'approved'::text, 'rejected'::text, 'disabled'::text, 'paused'::text, 'archived'::text, 'deleted'::text])),
  is_active boolean NOT NULL DEFAULT true,
  header_type text,
  components jsonb NOT NULL DEFAULT '[]'::jsonb,
  variables_schema jsonb NOT NULL DEFAULT '[]'::jsonb,
  example_payload jsonb NOT NULL DEFAULT '{}'::jsonb,
  rejection_reason text,
  quality_rating text,
  last_synced_at timestamp with time zone,
  external_created_at timestamp with time zone,
  external_updated_at timestamp with time zone,
  metadata jsonb NOT NULL DEFAULT '{}'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  meta_category text NOT NULL DEFAULT 'utility'::text,
  clasificacion_interna text,
  CONSTRAINT wp_whatsapp_templates_pkey PRIMARY KEY (id),
  CONSTRAINT wp_whatsapp_templates_empresa_id_fkey FOREIGN KEY (empresa_id) REFERENCES public.wp_empresa_perfil(id),
  CONSTRAINT wp_whatsapp_templates_numero_id_fkey FOREIGN KEY (numero_id) REFERENCES public.wp_numeros(id)
);